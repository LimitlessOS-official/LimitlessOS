# LimitlessOS

LimitlessOS is a clean-room operating system concept and bootstrap codebase focused on security, efficiency, stability, and clear AI safety boundaries.

## Current status: M5 safe installer and partition protection

M1 cleanup-final, M2 quarantine, M3 Product networking, M4 QEMU/QMP interactive GUI promotion, and M4.1 hardware-validation closure are accepted. M5 is `Safe Installer + Partition Protection`: it adds a host-side, raw-image verified installer safety path without adding installer code to the BIOS-constrained Product kernel.

Use the Product profile for the serious bootable slice:

```powershell
.\tools\build.ps1 -Architecture x86_64 -BuildProfile Product
```

Product currently boots to a persistent ring-3 shell and Product desktop, exposes only the Product apps/builtins/GUI apps, verifies disk/UEFI/ISO boot paths, proves reboot-surviving NVMe persistence through scoped write/commit authority, and exposes a `net` builtin for brokered network status when supported hardware is present. Current Product size is `473488` bytes, `925 / 1024` BIOS sectors, `99` BIOS reserve, checksum `0x5D996177`; the UEFI Product contract is `473488 / 2097152` bytes with `1623664` bytes reserve. The BIOS reserve is still below the 128-sector warning threshold and above the 96-sector hard floor.

Product apps are: APPEND, CAT, COPY, DELETE, LS, MKDIR, MOVE, RENAME, STAT, TOUCH, WRITE.

Product builtins are: apps, help, info, net, pwd.

Product GUI apps are: Terminal, File Manager, Settings.

Product networking is hardware-gated: UEFI/ISO paths with virtio-net or e1000e prove DHCP, DNS, TCP, and HTTP; BIOS/disk paths report cleanly unavailable. There is still no socket API and no ambient network access.

Product GUI authority is narrow: the compositor owns framebuffer presentation, the window manager owns focus/hit-testing/z-order, the input broker routes mouse/keyboard events, Terminal receives keyboard only while focused, File Manager is limited to brokered safe namespaces, and Settings is read-only system status. Apps do not receive raw framebuffer, raw input, ambient storage, or ambient network authority.

Manual MSI Cyborg 15 A13VE validation is tracked in `docs/hardware/msi-cyborg-15-a13ve.md`. Physical validation is pending user-supplied results because this workspace cannot boot the target laptop.

BIOS reserve safety for M4.1: reserve is only `99` sectors, so M5 installer code must not be added to the BIOS-constrained Product kernel unless reserve is recovered above the `128`-sector warning threshold or the boot contract is intentionally changed. UEFI remains governed by the `KERNEL64.BIN` byte budget, manifest/checksum correctness, placement/load correctness, and artifact inventory correctness; UEFI is not blocked by the BIOS `1024`-sector ceiling.

M5 installer safety is documented in `docs/installer/m5-safe-installer.md`. The installer dry-run lists GPT partitions, type GUIDs, labels, filesystem signatures, and safe/forbidden/unknown classification. Fixture verification proves dry-run no-writes, Windows ESP/NTFS/MSR/Recovery refusal, unknown FAT32/GPT refusal, dedicated LimitlessOS target acceptance, scoped write/format authority requirements, boot-entry authority separation, confirmation-token enforcement, and forbidden-partition unchanged checks. Real MSI internal NVMe writes remain disabled by default and are not product-approved until dry-run output is reviewed.

Unavailable or non-product in Product: ASK, ECHO, aliases, installer, package manager, and AI assistant behavior. These must not be presented as finished Product behavior.

Use the Experimental profile only for proof surfaces:

```powershell
.\tools\build.ps1 -Architecture x86_64 -BuildProfile Experimental
```

Experimental may initialize proof-only surfaces beyond the M4 Product GUI and broad hardware proof telemetry. Those surfaces are not Product unless separately promoted with runtime behavior, docs, and verification that agree.

Authoritative current status and evidence are in `docs/status.md`. The accepted M4 evidence pack is `dist/m4-evidence-20260511-190105/m4-evidence.json`; the M4.1 closure archive is `dist/m4-1-evidence-20260511-192857/m4-1-evidence.json`; the M5 evidence pack is `dist/m5-evidence-20260511-194236/m5-evidence.json`.

This first milestone provides:

- A bare-bones x86 BIOS boot path that assembles into a bootable disk image and now chunks kernel reads so the bootstrap image can grow past a single 64 KiB BIOS transfer window.
- A small protected-mode kernel that renders status text to VGA, serial, and a QEMU debug console.
- Early kernel-core services: boot info handoff, paging, physical frame tracking, IDT setup, PIC remap, PIT timer ticks, syscall dispatch, bootstrap IPC queues, priority-aware fair service scheduling, capability-guarded service routing, event-driven service wakeups, and a first multi-process ring-3 path with per-process address spaces, timer-preemptive switching, blocking sleep wakeups, registered multi-endpoint user IPC, kernel-managed user mailbox replies, bounded copied-payload direct user-to-user messaging through process-scoped revocable capability handles, kernel-namespaced service plus user endpoint-class discovery, explicit per-process discovery and scheduling profiles, weighted ring-3 dispatch with overdue latency picks, class-aware deadline-sensitive IPC wakeups, per-class CPU budgets, inversion-aware broker boosts, capability-admission limits, capability reuse plus table compaction after revokes and expirations, bounded broker dependency-depth propagation, AI-policy broker burst backpressure for non-interactive clients, service-graph route telemetry with queue high-water tracking, a build-generated bootstrap package archive with trusted signer checks plus v2 payload byte-range metadata that lets `init` launch named user-space services from verified manifests instead of entering fixed pre-manifest slots, a small multi-page bootstrap userspace loader, and a first brokered userspace filesystem and input path with synthetic `console`, `ramfs`, and `input` services, a larger startup input queue for longer seeded command streams, typed `ramfs-node` capabilities, explicit directory-create, directory-rename, directory-delete, node-stat, and file-write rights, a ring-3 startup shell with basic backspace and tab-aware line editing, current-directory tracking, explicit root-vs-cwd authority selection for absolute and relative paths, explicit source-directory and destination-directory authority for cross-directory moves, a filesystem-backed app descriptor path under `/APPS` for the Product utility set, descriptor-enforced authority masks plus explicit argument and launch-binding metadata that declare whether a launched app should receive only a shared buffer or also delegated base, destination, console, and input capabilities, and manifest-launched Product utilities that run through delegated directory, shared-buffer, and descriptor-bound service capabilities instead of bypassing the capability-mediated service path. Older `ask`, `echo`, and alias descriptors are quarantined from the Product profile.
- Foundational design docs for the hybrid kernel, installer, AI trust boundary, and roadmap.

## Current shape

The prototype is intentionally small. It is not yet a daily-use OS. It is the first buildable foundation for a future system with:

- A hybrid kernel with a small trusted core and a narrow set of in-kernel fast paths.
- A lightweight default install that can scale from older hardware to higher-end systems.
- A consent-gated AI assistant that is integrated deeply without being allowed uncontrolled autonomy.

The current most-complete image in this repository is still the 32-bit x86 bootstrap. The long-term product target is broader: a shared LimitlessOS capability, service, and package model that supports both legacy 32-bit x86 installs and 64-bit x86 systems, with the installer choosing the right kernel and compatibility profile for the detected hardware. That target now has a real boot split in the repo too: `tools\build.ps1 -Architecture x86_64` produces a BIOS long-mode scaffold image and report alongside the mature x86 BIOS image path, and that x64 lane now consumes the same boot-info contract as x86, enables the SSE/FPU state expected by the 64-bit C ABI, installs a kernel-owned GDT and TSS with explicit kernel/user selectors, configures the native `syscall` STAR selector pair from that measured descriptor plan, brings up a real x64 IDT, proves both software interrupt entry and PIT-driven IRQ wakeups, exposes a first queryable kernel ABI over `int 0x80`, proves a native long-mode `syscall` entry against that same scaffold data path, now executes from the higher-half kernel window at `0xFFFFFFFF80010000`, recovers from controlled breakpoint, invalid-opcode, and page-fault proofs while tracking last-fault state instead of treating every x64 exception as opaque halt-only behavior, now exposes the shared bootstrap package-archive summary on both the BIOS scaffold and the UEFI app path, now carries a first shared x64 service-namespace scaffold with package-aware service queries, now exposes an x64 principal registry for system, policy, console, and sealed service-worker identities, now binds bootstrap process records to those principals, endpoints, scheduler classes, capability budgets, signer-backed kernel-service manifests, and init-authorized one-shot launch-start lifecycle records, and now proves a tiny x64 service-capability handle lifecycle with principal-scoped handles, grant, short-lease attenuated delegation, route, revoke with child cascade, unknown-principal denial, wrong-owner denial, expired-handle denial, stale-handle denial, second-hop delegation denial, and over-broad authority denial through both interrupt-based and native syscall paths. The modern lane is starting to share real package, service, principal, process, launch, capability, and ring-transition signals with x86 instead of only boot mechanics. The repository now also emits install media across both lanes: the x86 path has a verified BIOS-bootable ISO generated through Windows IMAPI2, and the x86_64 path now has verified removable-media UEFI and UEFI optical ISO boot paths under OVMF in QEMU, including a required GOP framebuffer geometry plus bounded firmware draw/read-back proof, boot-info framebuffer metadata, a dedicated framebuffer page-directory at `0xB000`, a kernel-owned framebuffer marker draw/log proof, a staged `README.TXT` boot-media file read proof, a parsed `BOOTMAN.TXT` loader manifest, bounded `KERNEL64.BIN` load into an aligned 768 KiB handoff buffer, firmware-backed 2 MiB-aligned kernel placement with copied-byte verification, pre/post-placement firmware memory-map proofs, and a successful `ExitBootServices` proof that switches to serial/debug-only output, builds the boot handoff, and jumps into the linked x64 kernel, in addition to its raw BIOS scaffold image. The larger UEFI loader buffer is an explicitly verified headroom checkpoint for upcoming x64 storage work; it does not add disk, MMIO, DMA, filesystem, or driver authority.

The UEFI lane now builds the low boot-handoff tables at `0x1000`, writes boot-info plus optional framebuffer metadata at `0x9000`, maps a 16 MiB identity/high-half alias window for kernel runway, maps the GOP framebuffer through a dedicated page-directory at `0xB000`, reserves the next low handoff page at `0xC000` for broker-installed kernel MMIO page tables, stages a trampoline at `0xA000`, exits firmware services, and jumps into the linked x64 kernel at `0xFFFFFFFF80010000`. Verification now requires `jump-ready 1`, the kernel descriptor reload proof, the kernel-owned framebuffer handoff/draw proof when GOP is available, the compact sealed ring-3 bootstrap proofs through the second runtime page, and the disk-sourced descriptor/binary launch proofs before accepting either UEFI media path.

The x86_64 lifecycle scaffold now includes broker-owned capability drain and restart operations. Boot verification proves live endpoint handles block quiesce, proves an init-authorized broker drain can revoke those handles, denies restart until quiesce-ready, then rekeys the service runtime generation and derives a sealed four-page, 16 KiB persistent-shell runtime-bootstrap image plan from verified package payload metadata. The broker installs both a supervisor-only validation mapping and a separate read/execute user mapping at `0x41000000`, maps a user stack, and records page-count, table-index, protection-token, install-token, source-checksum, and entry-transfer telemetry without weakening the validation view. Ring-3 proofs now cover controlled entry, IF-enabled timer preemption capture, scheduler-owned task switching, shell-stream execution, and second-page filesystem/display mutation at `0x41001ED0`; utility command bodies now live in checksum-verified disk-sourced flat binaries loaded through the broker instead of duplicated inside the sealed image. Service capability handles are minted against runtime identity, so post-restart handles bind to the new generation while stale runtime tokens are rejected.

The x86_64 scheduler proof now goes one step beyond the one-way switch: a reusable `scheduler_x64` run-queue module saves task A as a complete interrupt frame, dispatches task B at `0x41000140` on stack `0x4001F800`, records B result `0x52514232`, restores task A, and completes with A result `0x52514131`. The run queue now also binds those saved frames to manifest-launched process identity, proving task A belongs to PID 2 `ai-policy` and task B belongs to PID 4 `console` with runtime and user-entry tokens. Scheduler registration can now accept a launched-process PID and resolve the broker-prepared entry frame through `process_x64`, while process syscalls expose the user-entry RIP/RSP/selectors/RFLAGS without callers reaching into raw launch records.

The x86_64 lane also now has its first userspace-practical filesystem, console, and input bridge: `fs_x64` reuses the shared RAMFS implementation behind the existing service-capability model, `console_x64` accepts only console service capabilities before writing user buffers, `input_x64` accepts only input service capabilities before copying raw command bytes or broker-normalized line-edited commands into writable user memory, and all three expose telemetry through the x64 syscall surface. Verification proves Product shell/file behavior through scoped capabilities, including read, stat, create, write, append, copy, move, rename, delete, and truthful unavailable handling for non-product surfaces. UEFI and ISO verification now also proves `drs-xhci`: an ECAM-discovered xHCI controller is mapped through a broker-private kernel MMIO aperture, page-aligned DCBAA/command/event/control/interrupt-ring memory is staged, the controller is reset and run, a USB slot is enabled and addressed, USB configuration plus HID report descriptors are read, a boot keyboard interrupt endpoint is configured, and bounded boot-protocol HID reports enter the same brokered input queue as PS/2. The `drs-mouse` checkpoint extends that input boundary with a separate broker-owned pointer queue fed by the 8042 second port, IRQ12, and xHCI boot-mouse detection. The build now enforces a dual boot contract: BIOS remains capped at 1024 sectors, while UEFI Product is checked as a 2 MiB kernel file against `BOOTMAN.TXT`.

The x86_64 input path now has hardware-backed keyboard and mouse event proofs staged behind the broker. IRQ1 PS/2 events are accepted into a bounded broker-owned keyboard queue, UEFI now performs a bounded 8042 bring-up with config-byte programming, first-port enable, and keyboard `0xF4` scanning enable before falling back to active polling, translated set-1 scancodes are preferred after PS/2 translation is enabled, set-2 decoding remains available, basic extended cursor/delete escape sequences are translated, authorized keyboard reads poll both PS/2 and xHCI, stale pending bytes are cleared on scancode interpretation switches or overlong fragments, and a GOP `INPUT DIAG` panel exposes xHCI, handoff, USB2, HID, PS/2, scan-enable, status/config/ACK, key-count, pending-count, last-scancode, and I2C-HID detection values for serial-free hardware debugging. The mouse side enables the 8042 second port, sends `0xD4 0xF4`, routes IRQ12 through PIC or IOAPIC as appropriate, parses 3-byte PS/2 packets, recognizes boot-protocol USB HID mouse interfaces, stores clamped absolute cursor coordinates plus button state, and queues pointer events separately from keyboard bytes. Verification still proves broker-side keyboard and mouse reads require scoped `input` authority; the older sealed hardware-command payloads were retired when the sealed image was compacted into a bootstrap stub.

That hardware-keyboard path now executes visible command-shaped work instead of stopping at byte validation: after the broker-side one-byte canary, ring-3 code reads complete lines through scoped `input` authority, writes prompts through scoped `console` authority, opens `/`, `/APPS`, `/APPS/INDEX.TXT`, `/APPS/CAT.APP`, and `/APPS/WRITE.APP` through scoped `ramfs` authority, lists root and app entries, prints descriptor-backed help and decoded info, reports `pwd`, reads/stats `README.TXT`, creates and rereads `HW.TXT`, revokes temporary node handles after use, and exits with `bye` only after all required proof flags are present. This keeps hardware input, filesystem access, and output all capability-mediated while producing externally visible text in raw BIOS, UEFI removable, and UEFI ISO verification.

Brokered keyboard reads still use bounded wait budgets and scoped input handles, preserving the same `input` capability boundary while leaving command execution to disk-sourced shell descriptors and utility binaries. After the scripted proof scaffold reaches `[x64] bootstrap halt`, x64 now enters the persistent ring-3 shell by default; the former `[x64:live]` brokered keyboard console remains only as a fallback diagnostic mode. The Product shell consumes the same brokered keyboard queue, normalizes typed paths for the RAMFS descriptor namespace, and routes `ls`, `cat`, `write`, `mkdir`, `stat`, `copy`, `delete`, `rename`, `move`, `touch`, `append`, `info`, and `net` through scoped capabilities instead of ambient filesystem or network authority. The xHCI poll path also opens MMIO write windows by the actual controller register virtual address, so later AHCI/NVMe/network MMIO mappings cannot stale the xHCI ERDP or doorbell write aperture.

The x86_64 display path now has its first userspace-visible hardware proof instead of only a firmware/kernel draw: the UEFI loader records GOP framebuffer metadata in boot-info, the kernel exposes a `display` service endpoint with a `SERVICE_CAP_DISPLAY` capability, and a sealed ring-3 second-page probe must obtain delegated display authority before drawing a bounded marker, clearing a kernel-bounded text panel, and rendering a tiny text line with the same verified `ring3 write ok\n` bytes it wrote through RAMFS. The brokered console service now also mirrors successful ring-3 console writes onto the GOP framebuffer with a small 5x7 font inside a bounded, line-cleared, scrolling console viewport, so the shell stream becomes visible on UEFI displays without giving shell processes direct framebuffer access or letting stale glyphs smear across shorter lines. BIOS boots report the same syscall path as unavailable instead of faking framebuffer support, while UEFI removable and ISO verification require positive display draw, pixel, clear, text-write, text-byte, console-mirror-write, console-mirror-byte, console line-clear, and console-scroll telemetry.

The x86_64 storage lane now has hardware-backed AHCI reads, read-only block routing, ISO9660 filesystem reads, disk-sourced utility launch, broker-private NVMe FAT32 mutation, and the first scoped caller-visible NVMe FAT32 write authority without bypassing the object-capability model. BIOS disk verification still requires a scoped block capability, wrong-owner denial, one authorized ATA PIO read of LBA0, a nonzero read token, and the `0x55AA` boot-sector signature. UEFI removable and ISO media now take the AHCI path further: after `drs-read` seals 2048 ATAPI bytes inside the broker, `drs-block` mints a block-worker-owned read-only block service capability, `drs-fs` consumes that block route inside the kernel storage layer to read ISO9660 sector 16 and root `README.TXT`, `drs-fs-user` delegates one scoped read-only filesystem capability to ring-3, `drs-fs-shell` dynamically scans the ISO `/APPS` directory before wiring the shell's disk-sourced `.APP` descriptors through that single delegated capability, `drs-load` reads and launches `/APPS/LS.BIN` from ISO media, and `drs-load-full` reads, checksum-verifies, registers, and selectively launches the full disk-sourced utility set including CAT/MKDIR/WRITE/RENAME/MOVE proofs while keeping ISO media read-only. The same ECAM-backed PCI lane now proves `drs-nvme-probe`, `drs-nvme-read`, `drs-nvme-gpt`, the expanded broker-private `drs-nvme-fat`, and `drs-nvme-rw`: UEFI/ISO media find an NVMe controller, map its 64-bit BAR0 through broker-private MMIO, initialize admin queues, submit Identify Controller, create broker-private IO queues, complete namespace reads into kernel-owned PRP buffers, read GPT LBA1/LBA2 and a FAT32 VBR from the deterministic NVMe sidecar, parse FAT32 BPB geometry, LFN directory entries, nested directories, and multi-cluster FAT chains, prove gated FAT32 create/update/delete with read-back, then mint and delegate exactly one scoped read-write FAT32 capability to the shell principal. Shell-created files are mirrored to NVMe only through that capability, with wrong-owner/stale/revoked denial, read-back checksum, audit count, commit count, and a two-boot same-image persistence proof; NVMe block endpoint publication, format authority, ambient filesystem authority, and additional filesystem capabilities remain zero.

The same lane now also inventories PCI configuration space without ambient hardware authority: BIOS media reports legacy IDE storage, while q35 UEFI media reports an AHCI controller address/vendor/class/BAR descriptor as a safe next-step target for a future brokered storage driver. Those inventory reads now go through a query-only `hardware-inventory` service capability, including explicit wrong-owner denials before authorized discovery telemetry is accepted. The AHCI path promotes that BAR into a separate brokered MMIO planner surface with base/span/flags/state/token telemetry, marks BIOS no-device boots as `safe-no-touch`/unmapped with unavailable controller, port, port-policy, AHCI read-plan, AHCI command-plan, memory-plan, table-prep, issue, bind, patch, publish, publication-gate, write-window, revocation-preflight, and write-window-open telemetry, and for q35 AHCI installs a broker-approved kernel-only/read-only/no-deref page-table view at `0xFFFFFFFF90000000` with PML4/PDPT/PD/PT indices `511/510/128/0`, cache-disabled/NX entry flags `0x8000000000000019`, and `map-installed 1`. Brokered read-only snapshots now cover AHCI HBA `CAP`, `GHC`, `PI`, and `VS`, followed by implemented/active port scanning and a read-only readiness classifier that records selected port `SSTS`, signature, command, task-file, command-issue, error, device kind, link detect/speed/power state, busy/DRQ state, and command-issue idleness. UEFI removable and ISO media prove an active ATAPI-signature port with `policy-ready 1`, then stage a non-executing ATAPI read-plan token plus command-layout token with bounded LBA/block metadata, command header/table geometry, CFIS/PRDT geometry, ATAPI packet size, opcode `0xA0`, packet opcode `0x28`, and transfer hint `2048`. They now also stage a broker-owned one-page command-memory preflight token describing command-list/header/table/PRDT/bounce-buffer offsets, a 2048-byte bounce window, PRDT byte count `2047`, and zero DMA address, then populate a non-issuing broker-private AHCI command table with header flags `0x00010025`, register FIS `0x27/0x80/0xA0`, ATAPI packet opcode `0x28`, PRDT byte count `2047`, checksum transition `0x76EFDDC5 -> 0x3FBFAF45`, and `table-written 1` while proving `read-armed 0`, `read-issued 0`, `read-dma 0`, `cmd-armed 0`, `cmd-issued 0`, `cmd-dma 0`, `mem-dma 0`, `mem-port-programmed 0`, `mem-armed 0`, `table-dma 0`, `table-port-programmed 0`, `table-armed 0`, and `table-issued 0`; BIOS proves zero read/command/memory/table authority. After the capability drain and handoff, a fresh block-worker-owned query-only hardware capability can now perform a driver-owned read-only probe of the same AHCI controller and selected ATAPI port readiness, bind that probe token into a driver-owned one-block read-intent descriptor, and stage a driver-owned zeroed read-buffer descriptor while preserving zero MMIO-write, port-program, DMA, publish, command, arm, media-read, filesystem-mint, and media-write side effects.

The latest x64 storage preflight now materializes the command-memory proof as a broker-owned, page-aligned, zeroed kernel page with virtual/physical telemetry, checksum `0x76EFDDC5`, `mem-zeroed 1`, and `mem-materialized 1` on UEFI/ISO AHCI paths. It then writes only a broker-private, non-issuing command-table skeleton into that page and records checksum `0x3FBFAF45`; it still exposes no user mapping, reports zero DMA addresses, leaves ports unprogrammed, issues no commands, and keeps BIOS/no-AHCI media on the unavailable zero-authority path. The follow-on controller-publication preflight computes the future CLB/CLBU/FB/FBU/CMD/CI port-register offsets and command-list/FIS values from that private page, proves the receive-FIS window at offset `1536` for `256` bytes, and still reports `publish-memory-written 0`, `publish-dma 0`, `publish-mmio-written 0`, `publish-port-programmed 0`, `publish-published 0`, `publish-command-issued 0`, and `publish-armed 0`. The publication gate then binds that publish token to the live hardware capability owner while explicitly blocking commitment: UEFI/ISO media report `gate-state 3`, `gate-flags 0x0000FFFF`, `gate-revocation-required 1`, `gate-revocation-satisfied 0`, `gate-write-window 0`, `gate-commit-allowed 0`, and zero MMIO, port, publish, command, or arm side effects; BIOS/no-AHCI media report unavailable `gate-flags 0x0001FFF9`. A second publish write-window policy checkpoint now sits after that gate: UEFI/ISO media bind both the gate token and publish token into `window-state 3`/`window-flags 0x0000FFFF` while proving `window-revocation-executed 0`, `window-write-window 0`, `window-commit-allowed 0`, and zero hardware side effects; BIOS/no-AHCI media report unavailable `window-flags 0x0001FFF1`. The next revocation preflight now binds the write-window token into `revoke-state 3`/`revoke-flags 0x0000FFFF`, records `revoke-live-before 1`, `revoke-live-after 1`, `revoke-planned 1`, `revoke-executed 0`, and `revoke-would-revoke 1`, but deliberately performs no capability revocation or hardware write yet; BIOS/no-AHCI media reports unavailable `revoke-flags 0x0001FFF1`. A final open-attempt checkpoint now proves the write window still cannot be opened until that revocation is actually enforced: UEFI/ISO media report `open-state 3`, `open-flags 0x0000FFFF`, revoke-token and window-token binding, `open-allowed 0`, `open-write-window 0`, `open-commit-allowed 0`, and zero MMIO, port, publish, command, or arm side effects, while BIOS/no-AHCI media reports unavailable `open-flags 0x0001FFF1`.

The follow-on AHCI exclusive-session checkpoint now binds the open, revoke, and window tokens into a future driver-ownership object without transferring ownership yet. UEFI/ISO media report `session-state 3`, `session-flags 0x0000FFFF`, live hardware handles present, revocation required and planned but not executed, `session-allowed 0`, `session-driver-owned 0`, disabled write window, denied commit, and zero MMIO, port, publish, command, or arm side effects; BIOS/no-AHCI media reports unavailable `session-flags 0x0001FFF1`.

The next AHCI checkpoint now executes the broker-side capability drain after the exclusive-session proof. UEFI/ISO media report `drain-state 3`, `drain-flags 0x0000FFFF`, session/open/revoke/window token binding, `drain-live-before 1`, `drain-revoked 1`, `drain-live-after 0`, `drain-revocation-executed 1`, and still prove write-window disabled, commit denied, and zero MMIO, port, publish, command, or arm side effects; BIOS/no-AHCI media report unavailable `drain-flags 0x0001FC71`.

The post-drain AHCI handoff checkpoint now turns that revocation into a reusable future driver boundary. UEFI/ISO media report `handoff-state 3`, `handoff-flags 0x0001FFFF`, the old hardware handle denied as stale, block-worker ownership `0x00001006`, a fresh query-only driver hardware capability, live handle count restored to one under the new owner, and continued zero write-window, commit, MMIO, port, publish, command, or arm side effects. BIOS/no-AHCI media report unavailable `handoff-flags 0x0003F861`, so the handoff path stays observable without manufacturing hardware authority.

The current AHCI driver-owned read-buffer checkpoint binds the read-intent token to a zeroed broker-private buffer descriptor without issuing it. UEFI/ISO media report `driver-buffer-state 3`, `driver-buffer-flags 0x0001FFFF`, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-buffer-read-bytes 2048`, `driver-buffer-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-buffer-zeroed 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read side effects. BIOS/no-AHCI media report unavailable `driver-buffer-flags 0x0003FC01`.

The AHCI driver read-execution gate checkpoint binds that read-buffer token into a locked future-read policy object while still denying execution under query-only authority. UEFI/ISO media report `driver-gate-state 3`, `driver-gate-flags 0x000FFFFF`, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-gate-read-bytes 2048`, `driver-gate-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-gate-exec-required 1`, `driver-gate-exec-granted 0`, `driver-gate-issue-allowed 0`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read side effects. BIOS/no-AHCI media report unavailable `driver-gate-flags 0x001FFC01`.

The AHCI driver read-execution attempt checkpoint consumes that locked gate only to prove the broker still refuses to issue the read under query-only authority. UEFI/ISO media report `driver-exec-state 3`, `driver-exec-flags 0x007FFFFF`, gate-token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-exec-read-bytes 2048`, `driver-exec-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-exec-attempted 1`, `driver-exec-granted 0`, `driver-exec-issue-allowed 0`, `driver-exec-issue-denied 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read side effects. BIOS/no-AHCI media report unavailable `driver-exec-flags 0x00DFFC01`.

The current AHCI driver read-result/export checkpoint consumes that denied execution token only to prove no media result can be exported under query-only authority. UEFI/ISO media report `driver-result-state 3`, `driver-result-flags 0x00FFFFFF`, exec-token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-result-read-bytes 2048`, `driver-result-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-result-zeroed 1`, `driver-result-read-ready 1`, `driver-result-granted 0`, `driver-result-denied 1`, `driver-result-bytes-available 0`, `driver-result-block-cap-minted 0`, `driver-result-fs-minted 0`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read side effects. BIOS/no-AHCI media report unavailable `driver-result-flags 0x01FFFC01`.

The current AHCI driver block-publication checkpoint consumes that denied result token only to prove it still cannot become an externally visible storage endpoint under query-only authority. UEFI/ISO media report `driver-publish-state 3`, `driver-publish-flags 0x07FFFFFF`, result-token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-publish-read-bytes 2048`, `driver-publish-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-publish-zeroed 1`, `driver-publish-read-ready 1`, `driver-publish-result-denied 1`, `driver-publish-bytes-available 0`, `driver-publish-block-endpoint 0`, `driver-publish-block-cap-minted 0`, `driver-publish-fs-minted 0`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `driver-publish-flags 0x0FFFFC01`.

The current AHCI driver read-authority checkpoint consumes that denied block-publication token only to prove it still cannot gain media-read authority under query-only hardware access. UEFI/ISO media report `drg-state 3`, `drg-flags 0x3FFFFFFF`, publish-token and result-token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drg-read-bytes 2048`, `drg-page-bytes 4096`, checksum `0x76EFDDC5`, `drg-zeroed 1`, `drg-ready 1`, `drg-media-auth 0`, `drg-block-endpoint 0`, `drg-block-cap 0`, `drg-fs-minted 0`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `drg-flags 0x7FFFF801`.

The AHCI driver media-read checkpoint consumes that denied read-authority token as an explicit media-read attempt and still denies it under query-only hardware access. UEFI/ISO media report `dmr-state 3`, `dmr-flags 0x0FFFFFFF`, read-grant token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `dmr-read-bytes 2048`, `dmr-page-bytes 4096`, checksum `0x76EFDDC5`, `dmr-drg-denied 1`, `dmr-auth 0`, `dmr-attempted 1`, `dmr-denied 1`, `dmr-bytes 0`, `dmr-block-endpoint 0`, `dmr-block-cap 0`, `dmr-fs-minted 0`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `dmr-flags 0x1FFFFC01`.

The AHCI driver read-completion checkpoint consumes that denied media-read token only to prove completion is still denied. UEFI/ISO media report `drc-state 3`, `drc-flags 0x1FFFFFFF`, media-read token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drc-read-bytes 2048`, `drc-page-bytes 4096`, checksum `0x76EFDDC5`, `drc-dmr-denied 1`, `drc-requested 1`, `drc-granted 0`, `drc-denied 1`, `drc-completed 0`, `drc-status 0`, `drc-bytes 0`, `drc-block-endpoint 0`, `drc-block-cap 0`, `drc-fs-minted 0`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `drc-flags 0x3FFFFC01`.

The AHCI driver read-capability checkpoint consumes that denied completion token only to prove no read-result capability can be minted under query-only hardware authority. UEFI/ISO media report `drcap-state 3`, `drcap-flags 0x0FFFFFFF`, completion-token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drcap-read-bytes 2048`, `drcap-page-bytes 4096`, checksum `0x76EFDDC5`, `drcap-drc-denied 1`, `drcap-requested 1`, `drcap-granted 0`, `drcap-denied 1`, `drcap-bytes 0`, `drcap-block-endpoint 0`, `drcap-block-cap 0`, `drcap-fs-minted 0`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `drcap-flags 0x1FFFFC01`.

The AHCI driver read-export checkpoint consumes that denied read-capability token only to prove no caller-visible read result can be exported under query-only hardware authority. UEFI/ISO media report `drx-state 3`, `drx-flags 0x3FFFFFFF`, read-cap token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drx-read-bytes 2048`, `drx-page-bytes 4096`, checksum `0x76EFDDC5`, `drx-drcap-denied 1`, `drx-requested 1`, `drx-granted 0`, `drx-denied 1`, `drx-bytes 0`, `drx-user-bytes 0`, `drx-user-buffer 0`, `drx-block-endpoint 0`, `drx-block-cap 0`, `drx-fs-minted 0`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `drx-flags 0x7FFFFC01`.

The AHCI driver read-response checkpoint consumes that denied read-export token only to prove no broker-facing block/read response can be produced under query-only hardware authority. UEFI/ISO media report `drr-state 3`, `drr-flags 0x3FFFFFFF`, read-export token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drr-read-bytes 2048`, `drr-page-bytes 4096`, checksum `0x76EFDDC5`, `drr-drx-denied 1`, `drr-requested 1`, `drr-granted 0`, `drr-denied 1`, `drr-bytes 0`, `drr-resp-bytes 0`, `drr-resp-status 0`, `drr-resp-checksum 0x00000000`, `drr-block-endpoint 0`, `drr-block-cap 0`, `drr-fs-minted 0`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `drr-flags 0x7FFFFC01`.

The AHCI driver read-delivery checkpoint consumes that denied read-response token only to prove no block-service delivery can produce broker-visible bytes or storage authority under query-only hardware authority. UEFI/ISO media report `drd-state 3`, `drd-flags 0x3FFFFFFF`, read-response token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drd-read-bytes 2048`, `drd-page-bytes 4096`, checksum `0x76EFDDC5`, `drd-drr-denied 1`, `drd-requested 1`, `drd-granted 0`, `drd-denied 1`, `drd-bytes 0`, `drd-deliv-bytes 0`, `drd-deliv-status 0`, `drd-deliv-checksum 0x00000000`, `drd-block-endpoint 0`, `drd-block-cap 0`, `drd-fs-minted 0`, `drd-buffer 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `drd-flags 0x7FFFFC01`.

The AHCI driver read-visibility checkpoint consumes that denied read-delivery token only to prove no service/client-visible read result can appear under query-only hardware authority. UEFI/ISO media report `drv-state 3`, `drv-flags 0x3FFFFFFF`, read-delivery token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drv-read-bytes 2048`, `drv-page-bytes 4096`, checksum `0x76EFDDC5`, `drv-drd-denied 1`, `drv-requested 1`, `drv-granted 0`, `drv-denied 1`, `drv-bytes 0`, `drv-vis-bytes 0`, `drv-vis-status 0`, `drv-vis-checksum 0x00000000`, `drv-block-endpoint 0`, `drv-block-cap 0`, `drv-fs-minted 0`, `drv-buffer 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `drv-flags 0x7FFFFC01`.

The AHCI driver read-commit checkpoint consumes that denied read-visibility token only to prove no committed read result can appear under query-only hardware authority. UEFI/ISO media report `drk-state 3`, `drk-flags 0x3FFFFFFF`, read-visible token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drk-read-bytes 2048`, `drk-page-bytes 4096`, checksum `0x76EFDDC5`, `drk-drv-denied 1`, `drk-requested 1`, `drk-granted 0`, `drk-denied 1`, `drk-bytes 0`, `drk-commit-bytes 0`, `drk-commit-status 0`, `drk-commit-checksum 0x00000000`, `drk-block-endpoint 0`, `drk-block-cap 0`, `drk-fs-minted 0`, `drk-buffer 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `drk-flags 0x7FFFFC01`.

The AHCI driver read-audit checkpoint consumes that denied read-commit token only to prove no audit/finalization surface can become bytes, status, checksum, block authority, filesystem authority, media access, or persistent storage mutation under query-only hardware authority. UEFI/ISO media report `dra-state 3`, `dra-flags 0x3FFFFFFF`, read-commit token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `dra-read-bytes 2048`, `dra-page-bytes 4096`, checksum `0x76EFDDC5`, `dra-drk-denied 1`, `dra-requested 1`, `dra-granted 0`, `dra-denied 1`, `dra-bytes 0`, `dra-audit-bytes 0`, `dra-audit-status 0`, `dra-audit-checksum 0x00000000`, `dra-block-endpoint 0`, `dra-block-cap 0`, `dra-fs-minted 0`, `dra-buffer 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `dra-flags 0x7FFFFC01`.

The AHCI driver read-upgrade checkpoint consumes that denied read-audit token only to prove audit/finalization evidence cannot be upgraded into real media-read authority. UEFI/ISO media report `dru-state 3`, `dru-flags 0x3FFFFFFF`, read-audit token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `dru-read-bytes 2048`, `dru-page-bytes 4096`, checksum `0x76EFDDC5`, `dru-dra-denied 1`, `dru-requested 1`, `dru-granted 0`, `dru-denied 1`, `dru-bytes 0`, `dru-up-cap 0xFFFFFFFF`, `dru-media-auth 0`, `dru-exec-auth 0`, `dru-block-endpoint 0`, `dru-block-cap 0`, `dru-fs-minted 0`, `dru-buffer 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `dru-flags 0x7FFFFC01`.

The AHCI driver read-activation checkpoint consumes that denied read-upgrade token only to prove controller-visible activation still cannot occur under query-only hardware authority. UEFI/ISO media report `dact-state 3`, `dact-flags 0x3FFFFFFF`, read-upgrade token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `dact-read-bytes 2048`, `dact-page-bytes 4096`, checksum `0x76EFDDC5`, `dact-dru-denied 1`, `dact-requested 1`, `dact-granted 0`, `dact-denied 1`, `dact-bytes 0`, `dact-act-cap 0xFFFFFFFF`, `dact-read-auth 0`, `dact-exec-auth 0`, `dact-block-endpoint 0`, `dact-block-cap 0`, `dact-fs-minted 0`, `dact-buffer 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `dact-flags 0x7FFFFC01`.

The AHCI driver read-arm checkpoint consumes that denied read-activation token only to prove controller arming still cannot occur under query-only hardware authority. UEFI/ISO media report `darm-state 3`, `darm-flags 0x3FFFFFFF`, read-activate token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `darm-read-bytes 2048`, `darm-page-bytes 4096`, checksum `0x76EFDDC5`, `darm-dact-denied 1`, `darm-requested 1`, `darm-granted 0`, `darm-denied 1`, `darm-bytes 0`, `darm-arm-cap 0xFFFFFFFF`, `darm-read-auth 0`, `darm-exec-auth 0`, `darm-block-endpoint 0`, `darm-block-cap 0`, `darm-fs-minted 0`, `darm-buffer 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `darm-flags 0x7FFFFC01`.

The AHCI driver read-submit checkpoint consumes that denied read-arm token only to prove command submission still cannot occur under query-only hardware authority. UEFI/ISO media report `dsub-state 3`, `dsub-flags 0x3FFFFFFF`, read-arm token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `dsub-read-bytes 2048`, `dsub-page-bytes 4096`, checksum `0x76EFDDC5`, `dsub-darm-denied 1`, `dsub-requested 1`, `dsub-granted 0`, `dsub-denied 1`, `dsub-bytes 0`, `dsub-submit-cap 0xFFFFFFFF`, `dsub-read-auth 0`, `dsub-exec-auth 0`, `dsub-block-endpoint 0`, `dsub-block-cap 0`, `dsub-fs-minted 0`, `dsub-buffer 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `dsub-flags 0x7FFFFC01`.

The AHCI driver read-permit checkpoint consumes that denied read-retire token only to prove read permission still cannot be granted under query-only hardware authority. UEFI/ISO media report `dprm-state 3`, `dprm-flags 0x3FFFFFFF`, read-retire token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `dprm-read-bytes 2048`, `dprm-page-bytes 4096`, checksum `0x76EFDDC5`, `dprm-dret-denied 1`, `dprm-requested 1`, `dprm-granted 0`, `dprm-denied 1`, `dprm-bytes 0`, `dprm-permit-cap 0xFFFFFFFF`, `dprm-read-auth 0`, `dprm-exec-auth 0`, `dprm-block-endpoint 0`, `dprm-block-cap 0`, `dprm-fs-minted 0`, `dprm-buffer 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `dprm-flags 0x7FFFFC01`.

The AHCI driver read-seal checkpoint consumes the denied read-close token only to prove no closed read response can become sealed, caller-visible media bytes or storage authority under query-only hardware access. UEFI/ISO media report `drsl-state 3`, `drsl-flags 0x3FFFFFFF`, read-close token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drsl-read-bytes 2048`, `drsl-page-bytes 4096`, checksum `0x76EFDDC5`, `drsl-drcl-denied 1`, `drsl-requested 1`, `drsl-granted 0`, `drsl-denied 1`, `drsl-bytes 0`, `drsl-status 0`, `drsl-seal-bytes 0`, `drsl-seal-checksum 0x00000000`, `drsl-seal-cap 0xFFFFFFFF`, `drsl-read-auth 0`, `drsl-exec-auth 0`, `drsl-block-endpoint 0`, `drsl-block-cap 0`, `drsl-fs-minted 0`, `drsl-buffer 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `drsl-flags 0x7FFFFC01`.

The AHCI driver read-unseal checkpoint consumes that denied read-seal token only to prove no sealed read can be unsealed into caller-visible bytes, status, checksum, or storage authority under query-only hardware access. UEFI/ISO media report `drul-state 3`, `drul-flags 0x3FFFFFFF`, drul-drsl-token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drul-read-bytes 2048`, `drul-page-bytes 4096`, checksum `0x76EFDDC5`, `drul-drsl-denied 1`, `drul-requested 1`, `drul-granted 0`, `drul-denied 1`, `drul-bytes 0`, `drul-status 0`, `drul-unseal-bytes 0`, `drul-unseal-checksum 0x00000000`, `drul-unseal-cap 0xFFFFFFFF`, `drul-read-auth 0`, `drul-exec-auth 0`, `drul-block-endpoint 0`, `drul-block-cap 0`, `drul-fs-minted 0`, `drul-buffer 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `drul-flags 0x7FFFFC01`.

The AHCI driver read-discard checkpoint consumes that denied read-unseal token only to prove the sealed/unsealed read lifecycle cannot be reanimated into caller-visible bytes, status, checksum, discard capability, or storage authority under query-only hardware access. UEFI/ISO media report `drdc-state 3`, `drdc-flags 0x3FFFFFFF`, read-unseal token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drdc-read-bytes 2048`, `drdc-page-bytes 4096`, checksum `0x76EFDDC5`, `drdc-drul-denied 1`, `drdc-requested 1`, `drdc-granted 0`, `drdc-denied 1`, `drdc-bytes 0`, `drdc-status 0`, `drdc-discard-bytes 0`, `drdc-discard-checksum 0x00000000`, `drdc-discard-cap 0xFFFFFFFF`, `drdc-read-auth 0`, `drdc-exec-auth 0`, `drdc-block-endpoint 0`, `drdc-block-cap 0`, `drdc-fs-minted 0`, `drdc-buffer 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `drdc-flags 0x7FFFFC01`.

The AHCI driver read-finalize checkpoint consumes that denied read-discard token only to prove a discarded read still cannot be finalized into caller-visible bytes, status, checksum, a finalize capability, read or execute authority, a block endpoint, block capability, filesystem authority, media bytes, or persistent storage mutation. UEFI/ISO media report `driver-read-finalize-state 3`, `driver-read-finalize-flags 0x3FFFFFFF`, read-discard token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-read-finalize-read-bytes 2048`, `driver-read-finalize-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-read-finalize-read-discard-denied 1`, `driver-read-finalize-requested 1`, `driver-read-finalize-granted 0`, `driver-read-finalize-denied 1`, `driver-read-finalize-bytes-available 0`, `driver-read-finalize-status 0`, `driver-read-finalize-finalized-bytes 0`, `driver-read-finalize-finalize-checksum 0x00000000`, `driver-read-finalize-finalize-cap 0xFFFFFFFF`, `driver-read-finalize-read-authority 0`, `driver-read-finalize-execute-authority 0`, `driver-read-finalize-block-endpoint 0`, `driver-read-finalize-block-cap-minted 0`, `driver-read-finalize-fs-minted 0`, `driver-read-finalize-buffer-unchanged 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `driver-read-finalize-flags 0x7FFFFC01`.

The AHCI driver read-authorize checkpoint consumes that denied read-finalize token only to prove the future "execute a read" authorization boundary still denies all policy grants under query-only hardware authority. UEFI/ISO media report `driver-read-authorize-state 3`, `driver-read-authorize-flags 0x3FFFFFFF`, read-finalize token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-read-authorize-read-bytes 2048`, `driver-read-authorize-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-read-authorize-read-finalize-denied 1`, `driver-read-authorize-requested 1`, `driver-read-authorize-granted 0`, `driver-read-authorize-denied 1`, `driver-read-authorize-policy-grant 0`, `driver-read-authorize-issue-authority 0`, `driver-read-authorize-dma-authority 0`, `driver-read-authorize-media-read-authority 0`, `driver-read-authorize-write-authority 0`, `driver-read-authorize-commit-authority 0`, `driver-read-authorize-block-endpoint 0`, `driver-read-authorize-block-cap-minted 0`, `driver-read-authorize-fs-minted 0`, `driver-read-authorize-buffer-unchanged 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `driver-read-authorize-flags 0x7FFFFC01`.

The AHCI driver read-dispatch checkpoint consumes that denied read-authorize token only to prove the driver-owned dispatch/queue boundary stays closed before any real read path exists. UEFI/ISO media report `driver-read-dispatch-state 3`, `driver-read-dispatch-flags 0x3FFFFFFF`, read-authorize token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-read-dispatch-read-bytes 2048`, `driver-read-dispatch-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-read-dispatch-read-authorize-denied 1`, `driver-read-dispatch-requested 1`, `driver-read-dispatch-granted 0`, `driver-read-dispatch-denied 1`, `driver-read-dispatch-policy-grant 0`, `driver-read-dispatch-dispatch-queued 0`, `driver-read-dispatch-queue-depth 0`, `driver-read-dispatch-issue-authority 0`, `driver-read-dispatch-dma-authority 0`, `driver-read-dispatch-media-read-authority 0`, `driver-read-dispatch-write-authority 0`, `driver-read-dispatch-commit-authority 0`, `driver-read-dispatch-block-endpoint 0`, `driver-read-dispatch-block-cap-minted 0`, `driver-read-dispatch-fs-minted 0`, `driver-read-dispatch-buffer-unchanged 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `driver-read-dispatch-flags 0x7FFFFC01`.

The AHCI driver read-queue checkpoint consumes that denied read-dispatch token only to prove queue admission stays closed before the block worker can be woken. UEFI/ISO media report `driver-read-queue-state 3`, `driver-read-queue-flags 0x3FFFFFFF`, read-dispatch token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-read-queue-read-bytes 2048`, `driver-read-queue-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-read-queue-read-dispatch-denied 1`, `driver-read-queue-requested 1`, `driver-read-queue-granted 0`, `driver-read-queue-denied 1`, `driver-read-queue-policy-grant 0`, `driver-read-queue-queue-inserted 0`, `driver-read-queue-queue-depth 0`, `driver-read-queue-worker-wake 0`, `driver-read-queue-issue-authority 0`, `driver-read-queue-dma-authority 0`, `driver-read-queue-media-read-authority 0`, `driver-read-queue-write-authority 0`, `driver-read-queue-commit-authority 0`, `driver-read-queue-block-endpoint 0`, `driver-read-queue-block-cap-minted 0`, `driver-read-queue-fs-minted 0`, `driver-read-queue-buffer-unchanged 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `driver-read-queue-flags 0x7FFFFC01`.

The AHCI driver read-worker checkpoint consumes that denied read-queue token only to prove block-worker admission still cannot dequeue or execute a request. UEFI/ISO media report `driver-read-worker-state 3`, `driver-read-worker-flags 0x3FFFFFFF`, read-queue token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-read-worker-read-bytes 2048`, `driver-read-worker-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-read-worker-read-queue-denied 1`, `driver-read-worker-requested 1`, `driver-read-worker-granted 0`, `driver-read-worker-denied 1`, `driver-read-worker-policy-grant 0`, `driver-read-worker-queue-inserted 0`, `driver-read-worker-queue-depth 0`, `driver-read-worker-worker-wake 0`, `driver-read-worker-worker-dequeued 0`, `driver-read-worker-issue-authority 0`, `driver-read-worker-dma-authority 0`, `driver-read-worker-media-read-authority 0`, `driver-read-worker-write-authority 0`, `driver-read-worker-commit-authority 0`, `driver-read-worker-block-endpoint 0`, `driver-read-worker-block-cap-minted 0`, `driver-read-worker-fs-minted 0`, `driver-read-worker-buffer-unchanged 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `driver-read-worker-flags 0x7FFFFC01`.

The AHCI driver read-schedule checkpoint consumes that denied read-worker token only to prove scheduler/run-queue admission stays closed. UEFI/ISO media report `driver-read-schedule-state 3`, `driver-read-schedule-flags 0x3FFFFFFF`, read-worker token binding, block-worker owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-read-schedule-read-bytes 2048`, `driver-read-schedule-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-read-schedule-read-worker-denied 1`, `driver-read-schedule-requested 1`, `driver-read-schedule-granted 0`, `driver-read-schedule-denied 1`, `driver-read-schedule-policy-grant 0`, `driver-read-schedule-queue-inserted 0`, `driver-read-schedule-queue-depth 0`, `driver-read-schedule-worker-wake 0`, `driver-read-schedule-worker-dequeued 0`, `driver-read-schedule-worker-runnable 0`, `driver-read-schedule-worker-scheduled 0`, `driver-read-schedule-issue-authority 0`, `driver-read-schedule-dma-authority 0`, `driver-read-schedule-media-read-authority 0`, `driver-read-schedule-write-authority 0`, `driver-read-schedule-commit-authority 0`, `driver-read-schedule-block-endpoint 0`, `driver-read-schedule-block-cap-minted 0`, `driver-read-schedule-fs-minted 0`, `driver-read-schedule-buffer-unchanged 1`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects. BIOS/no-AHCI media report unavailable `driver-read-schedule-flags 0x7FFFFC01`.

The current Product userspace path is intentionally narrower and more truthful than the older proof inventory: `session-shell` runs in ring 3, resolves `console`, `ramfs`, `input`, storage, and network status through brokered capability paths, blocks on a brokered input queue instead of polling, and exposes only the Product builtins and Product apps in `help` and `apps`. The Product app set is `append`, `cat`, `copy`, `delete`, `ls`, `mkdir`, `move`, `rename`, `stat`, `touch`, and `write`; `net` reports DHCP lease state when supported network hardware is present and reports `no network` otherwise. ASK, ECHO, legacy aliases, GUI, installer, package-manager, and AI surfaces are unavailable or experimental rather than presented as normal apps.

## Build

From the project root:

```powershell
.\tools\build.ps1
```

If PowerShell blocks local scripts on your machine, use:

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\build.ps1
```

That produces:

- `dist\limitlessos.img`
- `dist\limitlessos.iso`
- `dist\limitlessos.kernel.bin`

The x86 lane now produces both:

- a raw disk image for direct USB imaging or hard-disk-style QEMU boot
- a BIOS-bootable ISO for DVD or CD media and optical-style QEMU boot

You can also build the 64-bit scaffold target explicitly:

```powershell
.\tools\build.ps1 -Architecture x86_64
```

That path currently produces:

- `dist\limitlessos-x86_64.img`
- `dist\limitlessos-x86_64.iso`
- `dist\limitlessos-x86_64.efi`
- `dist\limitlessos-x86_64-uefi.img`
- `dist\limitlessos-x86_64-uefi\`
- `dist\limitlessos-x86_64.kernel.bin`
- `dist\limitlessos-x86_64.pe`
- `dist\limitlessos-x86_64.scaffold.bin`
- `dist\limitlessos-x86_64.scaffold.txt`

That path is now a real bootable BIOS long-mode scaffold. It is still intentionally small: it enters long mode, uses a chunked 127-sector BIOS loader so the scaffold can grow beyond the old single-transfer ceiling, enables a 16 MiB identity map through 4-level paging, aliases that same early mapping into the higher-half kernel window and now actually transfers execution into that higher-half entry, enables the SSE/FPU state needed by the 64-bit ABI, passes a shared boot-info structure into a tiny x86_64 kernel entry, validates that handoff, installs a measured long-mode descriptor table, brings up x64 interrupts/timers/syscalls, surfaces the shared bootstrap package archive, validates sealed service manifests, proves principal-scoped runtime-bound service capabilities, and runs brokered filesystem, console, input, display, block, PCI inventory, scheduler, and userspace shell proofs before halting cleanly. The build also packages two modern-media artifacts around that scaffold: a removable-media UEFI FAT image verified under OVMF in QEMU, and a matching x86_64 UEFI ISO that now also boots under OVMF in QEMU; both UEFI media paths locate GOP, load and verify `KERNEL64.BIN`, exit firmware services, jump into the x64 kernel, draw/log a kernel-owned framebuffer marker, and reach the compact bootstrap, second-page filesystem/display, real-media storage, and disk-sourced launch proofs with firmware services offline.

The x86_64 launch broker now also exposes guarded drain and restart requests for service lifecycle work. Drain is authorized like quiesce/stop, records observed and revoked capability counts, denies unsafe quiesce while live endpoint handles exist, and proves post-drain quiesce without adding a general userspace "revoke this endpoint" syscall. Restart is broker-authorized, denied until quiesce-ready, records restart generations, rekeys the manifest runtime token, refreshes a runtime image token from the verified package archive payload offset/size/checksum metadata, derives a sealed read/execute image plan rooted at the service image base, installs a real supervisor-only page-table mapping for validation, installs a separate read/execute user page-table view plus user stack mapping for the first ring-3 transfer, records protection profiles/tokens for those views, derives a transfer-ready ring-3 entry frame from the descriptor/TSS contract, executes a controlled `iretq` into the broker-prepared frame with interrupts masked, verifies the user stub can return through the syscall gate, repeats entry with IF set to prove timer IRQ frame capture from user mode, and now proves the interrupt handler can perform a scheduler-owned frame switch from one user task frame to another before returning to ring 3. Capability grant/delegate/route now preserve and enforce that runtime binding, so a handle cannot silently cross into a replacement service instance. The boot proof also reports drained/quiesce-ready service counts plus per-manifest phase, runtime identity, runtime-image identity, runtime-image-plan token, image-map token, protection token, user-entry token and denial, install token, entry-transfer token, table indexes, entry probe result, user transfer result, user preempt result, user switch result, and runtime-payload metadata across both interrupt-based and native x64 syscall paths.

The build now also generates a serialized bootstrap package archive from [packages/bootstrap-store.json](/C:/Users/h1nuz/Documents/Codex/2026-04-25/architecture-performance-use-a-hybrid-kernel-2/packages/bootstrap-store.json) through [tools/generate-package-store.ps1](/C:/Users/h1nuz/Documents/Codex/2026-04-25/architecture-performance-use-a-hybrid-kernel-2/tools/generate-package-store.ps1) before compiling the kernel. Archive v2 records payload offset, size, and checksum metadata so the kernel can verify the package catalog against measured bootstrap payloads instead of trusting manifest-only measurements; on the x86_64 lane, [tools/build.ps1](/C:/Users/h1nuz/Documents/Codex/2026-04-25/architecture-performance-use-a-hybrid-kernel-2/tools/build.ps1) first assembles [kernel/arch/x86_64/runtime_image_user.asm](/C:/Users/h1nuz/Documents/Codex/2026-04-25/architecture-performance-use-a-hybrid-kernel-2/kernel/arch/x86_64/runtime_image_user.asm) and passes the generated 16 KiB persistent-shell bootstrap image into the package archive generator so manifest payload metadata follows the sealed runtime image automatically. The same archive path now records measured payload slots for disk-sourced flat utility binaries staged under `/APPS`, so launch-broker verification checks real ISO bytes before mapping them.

If QEMU is installed later, you can run:

```powershell
.\tools\run-qemu.ps1
```

For a headless boot smoke test with captured kernel output:

```powershell
.\tools\verify-qemu.ps1
```

The QEMU helpers now support both architecture targets, and the x86 lane also supports optical-media boot verification:

```powershell
.\tools\run-qemu.ps1
.\tools\verify-qemu.ps1
.\tools\run-qemu.ps1 -BootMedia iso
.\tools\verify-qemu.ps1 -BootMedia iso
.\tools\run-qemu.ps1 -Architecture x86_64
.\tools\verify-qemu.ps1 -Architecture x86_64
.\tools\run-qemu.ps1 -Architecture x86_64 -BootMedia uefi
.\tools\verify-qemu.ps1 -Architecture x86_64 -BootMedia uefi
.\tools\run-qemu.ps1 -Architecture x86_64 -BootMedia iso
.\tools\verify-qemu.ps1 -Architecture x86_64 -BootMedia iso
```

The current media split is:

- `x86` raw image: verified
- `x86` ISO: verified
- `x86_64` raw image: verified
- `x86_64` UEFI removable image: verified with GOP framebuffer draw proof, boot-info framebuffer mapping, kernel-owned framebuffer draw proof, brokered display text proof, line-cleared scrolling console-to-framebuffer mirror proof, boot-media file read proof, bounded loader-buffer payload proof, firmware-backed kernel-placement proof, linked-base `0x10000` placement proof, pre/post-placement memory-map proof, and successful `ExitBootServices` proof
- `x86_64` UEFI ISO: verified with GOP framebuffer draw proof, boot-info framebuffer mapping, kernel-owned framebuffer draw proof, brokered display text proof, line-cleared scrolling console-to-framebuffer mirror proof, boot-media file read proof, bounded loader-buffer payload proof, firmware-backed kernel-placement proof, linked-base `0x10000` placement proof, pre/post-placement memory-map proof, and successful `ExitBootServices` proof

The current split is:

- `x86`: mature bootstrap path with kernel-core services, brokered filesystem/input, and the ring-3 shell plus utility stack
- `x86_64`: early bootable long-mode scaffold with BIOS, removable UEFI, and UEFI ISO media paths; higher-half kernel execution; measured descriptors, interrupts, timers, `int 0x80`, and native `syscall`; shared package, service, principal, process, launch, scheduler, filesystem, console, input, display, and block scaffolds; runtime-bound service capabilities with grant/delegate/route/revoke/lease-expiry and restart rekeying proofs; a sealed four-page 16 KiB ring-3 persistent-shell bootstrap image with supervisor-only validation and separate user executable mappings; controlled ring-3 entry, preemption, task-switch, filesystem/display, brokered input, brokered read-only LBA0 block-read, real AHCI/ATAPI read, read-only block, ISO9660, delegated read-only filesystem, and disk-sourced flat-binary launch proofs; brokered RAMFS utilities, shell-stream command handling, and a default post-scaffold interactive ring-3 shell; verified GOP framebuffer handoff/draw and console mirroring; and verified OVMF boot through both removable-media and optical ISO paths

## Documents

- `docs\architecture.md`
- `docs\security-model.md`
- `docs\installer.md`
- `docs\package-format.md`
- `docs\roadmap.md`

## Important security note

One requirement was that the AI assistant must not be viewable, editable, or hackable by users under any circumstance. No honest system design can guarantee absolute immunity against every attacker, especially with physical access, firmware compromise, or supply-chain compromise. This codebase therefore treats that as a tamper-resistance and isolation target, not a magic promise.

The design response is:

- hardware-rooted boot trust
- signed and measured system components
- isolated assistant runtime boundaries
- explicit user consent for action-taking
- remote attestation for sensitive AI functions
- a visible audit trail for every privileged action

## Latest storage checkpoint

The current latest storage-hardware checkpoint is `drs-nvme-rw`. UEFI removable and ISO verification attach a deterministic GPT/FAT32 NVMe sidecar generated by `tools\generate-nvme-image.ps1`, discover it through the ECAM scanner, map BAR0 even when firmware places it above 4 GiB, initialize broker-private admin submission/completion queues, enable the controller, submit one Identify Controller command, require a nonempty model/firmware string, create one broker-private IO completion/submission queue pair, submit one namespace-1 LBA0 NVM Read into a kernel-owned 4 KiB PRP buffer, bind to that sealed read token to validate the GPT header at LBA 1, find the FAT32/basic-data partition entry at LBA 2, confirm start LBA `2048`, validate the partition VBR `0x55AA` signature, parse the BPB, and exercise the FAT reader and writer against staged fixtures. The accepted UEFI/ISO proof reports `drs-nvme-fat-bytes-per-sector 512`, `drs-nvme-fat-sectors-per-cluster 2`, reconstructs the ASCII LFN for `Limitless Long Name.txt`, reconstructs the UTF-16 LFN for `Caf\u00E9.txt` through UTF-8 path matching, traverses `/APPS/DATA/FILE.TXT`, follows the multi-cluster `MULTI.BIN` chain for `2500` bytes, keeps the original `NVME.TXT` checksum `0xD4F8C331`, then opens only a broker-private mutation gate to allocate cluster `12` for a new LFN file, extend `NVME.TXT` through cluster `13`, tombstone and free `/REMOVE.ME` on cluster `14`, issue `16` dirty-sector NVMe flushes, and read back the created and updated content checksums `0x8B4D45D8` and `0x4E5F0AAE`. The new `drs-nvme-rw` step mints one persistent, scoped read-write FAT32 capability for the shell principal, proves wrong-owner, stale-handle, and revoked-handle denial, writes `SHELL.TXT` through the shell path, reads it back with checksum `0x46A2678A`, records audit and commit counters, and verifies persistence with `tools\verify-nvme-persistence.ps1` by booting the same writable NVMe image twice and requiring the second boot to report `persisted 1`. BIOS and raw disk paths report NVMe/GPT/FAT/RW unavailable. The proof still reports zero NVMe block endpoint publication, zero format authority, zero ambient filesystem authority, and zero additional filesystem capabilities beyond that one scoped shell delegation.

The x86_64 AHCI lane now has a non-issuing command-issue preflight after broker-private command-table preparation. UEFI removable and ISO verification require `issue-state 3`, `issue-flags 0x06FFFFFF`, table/checksum token binding, idle command slot, ready TFD/SERR policy, recorded stop/start and timeout policy, and continued `issue-command-issued 0`; BIOS/no-AHCI media reports the matching unavailable proof with `issue-flags 0x073F0001`.

The newest preflight also stages a predicted-only AHCI address-bind proof. UEFI removable and ISO media compute future command-list, command-table, and bounce-buffer physical addresses, predicted CTBA/PRDT fields, and a page-address-derived checksum while keeping `bind-memory-written 0`, `bind-published 0`, and `bind-command-issued 0`; BIOS/no-AHCI media reports unavailable `bind-flags 0x1EFF0001`.

The follow-on stage applies that address bind only inside the broker-owned private command page. UEFI removable and ISO media now prove `patch-state 3`, `patch-flags 0x0DFFFFFF`, checksum transition from `0x3FBFAF45` to the page-address-derived private-patch checksum, and `patch-memory-written 1` while still keeping `patch-dma 0`, `patch-port-programmed 0`, `patch-published 0`, and `patch-command-issued 0`; BIOS/no-AHCI media reports unavailable `patch-flags 0x03EFE001`.

The newest storage checkpoint stages controller publication as a non-writing preflight only. UEFI removable and ISO media now prove `publish-state 3`, `publish-flags 0x03FFFFFF`, token binding back to patch/bind/issue/table/memory/command/read plans, computed CLB/FB register offsets and values, receive-FIS geometry `1536/256`, page-checksum match, alignment/range/below-4G checks, and continued zero memory/DMA/MMIO/port/publish/command/arm side effects; BIOS/no-AHCI media reports unavailable `publish-flags 0x07DFE001`.

The newest policy checkpoint adds an AHCI exclusive-session object after the denied write-window-open proof. UEFI removable and ISO media now prove `session-state 3`, `session-flags 0x0000FFFF`, open/revoke/window token binding, live hardware handles present, required-but-unexecuted revocation, denied driver ownership, disabled write window, denied commit, and continued zero MMIO/port/publish/command/arm side effects; BIOS/no-AHCI media reports unavailable `session-flags 0x0001FFF1`.

The newest drain checkpoint executes the revocation boundary only after all prior read-only/session telemetry is captured. UEFI removable and ISO media now prove `drain-state 3`, `drain-flags 0x0000FFFF`, session/open/revoke/window token binding, `drain-live-before 1`, `drain-revoked 1`, `drain-live-after 0`, `drain-revocation-executed 1`, disabled write window, denied commit, and continued zero MMIO/port/publish/command/arm side effects; BIOS/no-AHCI media reports unavailable `drain-flags 0x0001FC71`.

The newest handoff checkpoint regrants hardware authority only after that drain. UEFI removable and ISO media now prove `handoff-state 3`, `handoff-flags 0x0001FFFF`, stale denial of the old hardware handle, a fresh block-worker-owned query-only hardware capability, `handoff-live-before 0`, `handoff-live-after 1`, and continued zero write-window/commit/MMIO/port/publish/command/arm side effects; BIOS/no-AHCI media reports unavailable `handoff-flags 0x0003F861`.

The newest driver-owned read-only probe consumes that fresh query-only hardware capability without widening authority. UEFI removable and ISO media now prove `driver-probe-state 3`, `driver-probe-flags 0x0001FFFF`, handoff-token binding, owner `0x00001006`, query-only binding, controller `CAP/GHC/PI/VS` reads, selected ATAPI port readiness, `driver-probe-op 2`, `driver-probe-blocks 1`, `driver-probe-bytes 2048`, and zero MMIO-write/port-program/publish/command/DMA/arm side effects; BIOS/no-AHCI media reports unavailable `driver-probe-flags 0x0003FC01`.

The newest read-intent checkpoint consumes that driver probe token without crossing into command issue. UEFI removable and ISO media now prove `driver-intent-state 3`, `driver-intent-flags 0x0001FFFF`, probe-token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, `driver-intent-lba 0`, `driver-intent-blocks 1`, `driver-intent-bytes 2048`, and zero MMIO-write/port-program/publish/command/DMA/arm/media-read side effects; BIOS/no-AHCI media reports unavailable `driver-intent-flags 0x0003FC01`.

The newest read-buffer checkpoint consumes that read-intent token without crossing into command issue or media access. UEFI removable and ISO media now prove `driver-buffer-state 3`, `driver-buffer-flags 0x0001FFFF`, intent-token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-buffer-read-bytes 2048`, `driver-buffer-page-bytes 4096`, zero-page checksum `0x76EFDDC5`, `driver-buffer-zeroed 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read side effects; BIOS/no-AHCI media reports unavailable `driver-buffer-flags 0x0003FC01`.

The newest read-execution gate checkpoint consumes that read-buffer token without granting execution. UEFI removable and ISO media now prove `driver-gate-state 3`, `driver-gate-flags 0x000FFFFF`, buffer-token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-gate-read-bytes 2048`, `driver-gate-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-gate-zeroed 1`, `driver-gate-read-ready 1`, `driver-gate-exec-required 1`, `driver-gate-exec-granted 0`, `driver-gate-issue-allowed 0`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read side effects; BIOS/no-AHCI media reports unavailable `driver-gate-flags 0x001FFC01`.

The read-execution attempt checkpoint consumes that locked gate only to prove execution remains denied. UEFI removable and ISO media now prove `driver-exec-state 3`, `driver-exec-flags 0x007FFFFF`, gate-token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-exec-read-bytes 2048`, `driver-exec-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-exec-attempted 1`, `driver-exec-required 1`, `driver-exec-granted 0`, `driver-exec-issue-allowed 0`, `driver-exec-issue-denied 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read side effects; BIOS/no-AHCI media reports unavailable `driver-exec-flags 0x00DFFC01`.

The newest read-result/export checkpoint consumes that denied execution token without exporting media bytes or delegated storage authority. UEFI removable and ISO media now prove `driver-result-state 3`, `driver-result-flags 0x00FFFFFF`, exec-token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-result-read-bytes 2048`, `driver-result-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-result-zeroed 1`, `driver-result-read-ready 1`, `driver-result-granted 0`, `driver-result-denied 1`, `driver-result-bytes-available 0`, `driver-result-block-cap-minted 0`, `driver-result-fs-minted 0`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read side effects; BIOS/no-AHCI media reports unavailable `driver-result-flags 0x01FFFC01`.

The newest block-publication checkpoint consumes that denied result token without publishing a block endpoint or minting storage authority. UEFI removable and ISO media now prove `driver-publish-state 3`, `driver-publish-flags 0x07FFFFFF`, result-token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-publish-read-bytes 2048`, `driver-publish-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-publish-zeroed 1`, `driver-publish-read-ready 1`, `driver-publish-result-denied 1`, `driver-publish-bytes-available 0`, `driver-publish-block-endpoint 0`, `driver-publish-block-cap-minted 0`, `driver-publish-fs-minted 0`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `driver-publish-flags 0x0FFFFC01`.

The newest read-authority checkpoint consumes that denied block-publication token without granting media-read authority. UEFI removable and ISO media now prove `drg-state 3`, `drg-flags 0x3FFFFFFF`, publish-token and result-token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drg-read-bytes 2048`, `drg-page-bytes 4096`, checksum `0x76EFDDC5`, `drg-zeroed 1`, `drg-ready 1`, `drg-media-auth 0`, `drg-block-endpoint 0`, `drg-block-cap 0`, `drg-fs-minted 0`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drg-flags 0x7FFFF801`.

The newest denied media-read checkpoint consumes that denied read-authority token without reading media or exporting bytes. UEFI removable and ISO media now prove `dmr-state 3`, `dmr-flags 0x0FFFFFFF`, read-grant token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `dmr-read-bytes 2048`, `dmr-page-bytes 4096`, checksum `0x76EFDDC5`, `dmr-drg-denied 1`, `dmr-auth 0`, `dmr-attempted 1`, `dmr-denied 1`, `dmr-bytes 0`, `dmr-block-endpoint 0`, `dmr-block-cap 0`, `dmr-fs-minted 0`, `dmr-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `dmr-flags 0x1FFFFC01`.

The read-completion checkpoint consumes that denied media-read token without completing a read or exporting status bytes. UEFI removable and ISO media now prove `drc-state 3`, `drc-flags 0x1FFFFFFF`, media-read token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drc-read-bytes 2048`, `drc-page-bytes 4096`, checksum `0x76EFDDC5`, `drc-dmr-denied 1`, `drc-requested 1`, `drc-granted 0`, `drc-denied 1`, `drc-completed 0`, `drc-status 0`, `drc-bytes 0`, `drc-block-endpoint 0`, `drc-block-cap 0`, `drc-fs-minted 0`, `drc-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drc-flags 0x3FFFFC01`.

The read-capability checkpoint consumes that denied completion token without granting a read-result capability or exporting storage authority. UEFI removable and ISO media now prove `drcap-state 3`, `drcap-flags 0x0FFFFFFF`, completion-token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drcap-read-bytes 2048`, `drcap-page-bytes 4096`, checksum `0x76EFDDC5`, `drcap-drc-denied 1`, `drcap-requested 1`, `drcap-granted 0`, `drcap-denied 1`, `drcap-bytes 0`, `drcap-block-endpoint 0`, `drcap-block-cap 0`, `drcap-fs-minted 0`, `drcap-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drcap-flags 0x1FFFFC01`.

The newest read-export checkpoint consumes that denied read-capability token without exporting data into any caller-visible buffer. UEFI removable and ISO media now prove `drx-state 3`, `drx-flags 0x3FFFFFFF`, read-cap token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drx-read-bytes 2048`, `drx-page-bytes 4096`, checksum `0x76EFDDC5`, `drx-drcap-denied 1`, `drx-requested 1`, `drx-granted 0`, `drx-denied 1`, `drx-bytes 0`, `drx-user-bytes 0`, `drx-user-buffer 0`, `drx-block-endpoint 0`, `drx-block-cap 0`, `drx-fs-minted 0`, `drx-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drx-flags 0x7FFFFC01`.

The read-response checkpoint consumes that denied read-export token without constructing any broker-facing block/read response. UEFI removable and ISO media now prove `drr-state 3`, `drr-flags 0x3FFFFFFF`, read-export token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drr-read-bytes 2048`, `drr-page-bytes 4096`, checksum `0x76EFDDC5`, `drr-drx-denied 1`, `drr-requested 1`, `drr-granted 0`, `drr-denied 1`, `drr-bytes 0`, `drr-resp-bytes 0`, `drr-resp-status 0`, `drr-resp-checksum 0x00000000`, `drr-block-endpoint 0`, `drr-block-cap 0`, `drr-fs-minted 0`, `drr-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drr-flags 0x7FFFFC01`.

The read-delivery checkpoint consumes that denied read-response token without delivering block-service-visible bytes or authority. UEFI removable and ISO media now prove `drd-state 3`, `drd-flags 0x3FFFFFFF`, read-response token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drd-read-bytes 2048`, `drd-page-bytes 4096`, checksum `0x76EFDDC5`, `drd-drr-denied 1`, `drd-requested 1`, `drd-granted 0`, `drd-denied 1`, `drd-bytes 0`, `drd-deliv-bytes 0`, `drd-deliv-status 0`, `drd-deliv-checksum 0x00000000`, `drd-block-endpoint 0`, `drd-block-cap 0`, `drd-fs-minted 0`, `drd-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drd-flags 0x7FFFFC01`.

The read-commit checkpoint consumes that denied read-visibility token without committing block-service/client-visible bytes or authority. UEFI removable and ISO media now prove `drk-state 3`, `drk-flags 0x3FFFFFFF`, read-visible token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drk-read-bytes 2048`, `drk-page-bytes 4096`, checksum `0x76EFDDC5`, `drk-drv-denied 1`, `drk-requested 1`, `drk-granted 0`, `drk-denied 1`, `drk-bytes 0`, `drk-commit-bytes 0`, `drk-commit-status 0`, `drk-commit-checksum 0x00000000`, `drk-block-endpoint 0`, `drk-block-cap 0`, `drk-fs-minted 0`, `drk-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drk-flags 0x7FFFFC01`.

The read-audit checkpoint consumes that denied read-commit token without allowing audit/finalization telemetry to become storage bytes or authority. UEFI removable and ISO media now prove `dra-state 3`, `dra-flags 0x3FFFFFFF`, read-commit token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `dra-read-bytes 2048`, `dra-page-bytes 4096`, checksum `0x76EFDDC5`, `dra-drk-denied 1`, `dra-requested 1`, `dra-granted 0`, `dra-denied 1`, `dra-bytes 0`, `dra-audit-bytes 0`, `dra-audit-status 0`, `dra-audit-checksum 0x00000000`, `dra-block-endpoint 0`, `dra-block-cap 0`, `dra-fs-minted 0`, `dra-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `dra-flags 0x7FFFFC01`.

The read-upgrade checkpoint consumes that denied read-audit token without allowing audit/finalization evidence to upgrade into media-read authority. UEFI removable and ISO media now prove `dru-state 3`, `dru-flags 0x3FFFFFFF`, read-audit token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `dru-read-bytes 2048`, `dru-page-bytes 4096`, checksum `0x76EFDDC5`, `dru-dra-denied 1`, `dru-requested 1`, `dru-granted 0`, `dru-denied 1`, `dru-bytes 0`, `dru-up-cap 0xFFFFFFFF`, `dru-media-auth 0`, `dru-exec-auth 0`, `dru-block-endpoint 0`, `dru-block-cap 0`, `dru-fs-minted 0`, `dru-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `dru-flags 0x7FFFFC01`.

The read-arm checkpoint consumes the denied read-activation token without allowing that activation denial to become an armed controller capability or storage authority. UEFI removable and ISO media now prove `darm-state 3`, `darm-flags 0x3FFFFFFF`, read-activate token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `darm-read-bytes 2048`, `darm-page-bytes 4096`, checksum `0x76EFDDC5`, `darm-dact-denied 1`, `darm-requested 1`, `darm-granted 0`, `darm-denied 1`, `darm-bytes 0`, `darm-arm-cap 0xFFFFFFFF`, `darm-read-auth 0`, `darm-exec-auth 0`, `darm-block-endpoint 0`, `darm-block-cap 0`, `darm-fs-minted 0`, `darm-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `darm-flags 0x7FFFFC01`.

The read-submit checkpoint consumes the denied read-arm token without allowing that arming denial to become a submitted command capability or storage authority. UEFI removable and ISO media now prove `dsub-state 3`, `dsub-flags 0x3FFFFFFF`, read-arm token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `dsub-read-bytes 2048`, `dsub-page-bytes 4096`, checksum `0x76EFDDC5`, `dsub-darm-denied 1`, `dsub-requested 1`, `dsub-granted 0`, `dsub-denied 1`, `dsub-bytes 0`, `dsub-submit-cap 0xFFFFFFFF`, `dsub-read-auth 0`, `dsub-exec-auth 0`, `dsub-block-endpoint 0`, `dsub-block-cap 0`, `dsub-fs-minted 0`, `dsub-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `dsub-flags 0x7FFFFC01`.

The read-observe checkpoint consumes the denied read-submit token without allowing that submission denial to become a completion observation, result status, media bytes, or storage authority. UEFI removable and ISO media now prove `dobs-state 3`, `dobs-flags 0x3FFFFFFF`, read-submit token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `dobs-read-bytes 2048`, `dobs-page-bytes 4096`, checksum `0x76EFDDC5`, `dobs-dsub-denied 1`, `dobs-requested 1`, `dobs-granted 0`, `dobs-denied 1`, `dobs-bytes 0`, `dobs-obs-status 0`, `dobs-obs-bytes 0`, `dobs-obs-checksum 0x00000000`, `dobs-read-auth 0`, `dobs-exec-auth 0`, `dobs-block-endpoint 0`, `dobs-block-cap 0`, `dobs-fs-minted 0`, `dobs-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `dobs-flags 0x7FFFFC01`.

The read-retire checkpoint consumes that denied read-observe token without allowing completion retirement to become result status, media bytes, or storage authority. UEFI removable and ISO media prove `dret-state 3`, `dret-flags 0x3FFFFFFF`, read-observe token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `dret-read-bytes 2048`, `dret-page-bytes 4096`, checksum `0x76EFDDC5`, `dret-dobs-denied 1`, `dret-requested 1`, `dret-granted 0`, `dret-denied 1`, `dret-bytes 0`, `dret-ret-status 0`, `dret-ret-bytes 0`, `dret-ret-checksum 0x00000000`, `dret-read-auth 0`, `dret-exec-auth 0`, `dret-block-endpoint 0`, `dret-block-cap 0`, `dret-fs-minted 0`, `dret-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `dret-flags 0x7FFFFC01`.

The read-permit checkpoint consumes that denied read-retire token without allowing read permission, media bytes, or storage authority. UEFI removable and ISO media prove `dprm-state 3`, `dprm-flags 0x3FFFFFFF`, read-retire token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `dprm-read-bytes 2048`, `dprm-page-bytes 4096`, checksum `0x76EFDDC5`, `dprm-dret-denied 1`, `dprm-requested 1`, `dprm-granted 0`, `dprm-denied 1`, `dprm-bytes 0`, `dprm-permit-cap 0xFFFFFFFF`, `dprm-read-auth 0`, `dprm-exec-auth 0`, `dprm-block-endpoint 0`, `dprm-block-cap 0`, `dprm-fs-minted 0`, `dprm-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `dprm-flags 0x7FFFFC01`.

The read-window checkpoint consumes that denied read-permit token without opening a caller-visible read window or minting storage authority. UEFI removable and ISO media prove `dwin-state 3`, `dwin-flags 0x3FFFFFFF`, read-permit token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `dwin-read-bytes 2048`, `dwin-page-bytes 4096`, checksum `0x76EFDDC5`, `dwin-zeroed 1`, `dwin-ready 1`, `dwin-dprm-denied 1`, `dwin-requested 1`, `dwin-granted 0`, `dwin-denied 1`, `dwin-bytes 0`, `dwin-window-cap 0xFFFFFFFF`, `dwin-open 0`, `dwin-read-auth 0`, `dwin-exec-auth 0`, `dwin-block-endpoint 0`, `dwin-block-cap 0`, `dwin-fs-minted 0`, `dwin-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `dwin-flags 0x7FFFFC01`.

The read-lease checkpoint consumes that denied read-window token without creating a reusable read lease or minting storage authority. UEFI removable and ISO media prove `dlse-state 3`, `dlse-flags 0x3FFFFFFF`, read-window token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `dlse-read-bytes 2048`, `dlse-page-bytes 4096`, checksum `0x76EFDDC5`, `dlse-zeroed 1`, `dlse-ready 1`, `dlse-dwin-denied 1`, `dlse-requested 1`, `dlse-granted 0`, `dlse-denied 1`, `dlse-bytes 0`, `dlse-lease-cap 0xFFFFFFFF`, `dlse-active 0`, `dlse-read-auth 0`, `dlse-exec-auth 0`, `dlse-block-endpoint 0`, `dlse-block-cap 0`, `dlse-fs-minted 0`, `dlse-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `dlse-flags 0x7FFFFC01`.

The read-use checkpoint consumes that denied read-lease token without allowing a lease-use operation to become media bytes or storage authority. UEFI removable and ISO media prove `duse-state 3`, `duse-flags 0x3FFFFFFF`, read-lease token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `duse-read-bytes 2048`, `duse-page-bytes 4096`, checksum `0x76EFDDC5`, `duse-zeroed 1`, `duse-ready 1`, `duse-dlse-denied 1`, `duse-requested 1`, `duse-granted 0`, `duse-denied 1`, `duse-bytes 0`, `duse-use-cap 0xFFFFFFFF`, `duse-active 0`, `duse-read-auth 0`, `duse-exec-auth 0`, `duse-block-endpoint 0`, `duse-block-cap 0`, `duse-fs-minted 0`, `duse-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `duse-flags 0x7FFFFC01`.

The read-report checkpoint consumes that denied read-use token only as a post-use report request denial. UEFI removable and ISO media prove `drpt-state 3`, `drpt-flags 0x3FFFFFFF`, read-use token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drpt-read-bytes 2048`, `drpt-page-bytes 4096`, checksum `0x76EFDDC5`, `drpt-duse-denied 1`, `drpt-requested 1`, `drpt-granted 0`, `drpt-denied 1`, `drpt-bytes 0`, `drpt-status 0`, `drpt-report-bytes 0`, `drpt-report-checksum 0x00000000`, `drpt-report-cap 0xFFFFFFFF`, `drpt-block-endpoint 0`, `drpt-block-cap 0`, `drpt-fs-minted 0`, `drpt-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drpt-flags 0x7FFFFC01`.

The read-receipt checkpoint consumes that denied read-report token only as a no-success-receipt denial. UEFI removable and ISO media prove `drrc-state 3`, `drrc-flags 0x3FFFFFFF`, read-report token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drrc-read-bytes 2048`, `drrc-page-bytes 4096`, checksum `0x76EFDDC5`, `drrc-drpt-denied 1`, `drrc-requested 1`, `drrc-granted 0`, `drrc-denied 1`, `drrc-bytes 0`, `drrc-status 0`, `drrc-receipt-bytes 0`, `drrc-receipt-checksum 0x00000000`, `drrc-receipt-cap 0xFFFFFFFF`, `drrc-block-endpoint 0`, `drrc-block-cap 0`, `drrc-fs-minted 0`, `drrc-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drrc-flags 0x7FFFFC01`.

The read-ack checkpoint consumes that denied read-receipt token only as a no-success-ack denial. UEFI removable and ISO media prove `drak-state 3`, `drak-flags 0x3FFFFFFF`, read-receipt token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drak-read-bytes 2048`, `drak-page-bytes 4096`, checksum `0x76EFDDC5`, `drak-drrc-denied 1`, `drak-requested 1`, `drak-granted 0`, `drak-denied 1`, `drak-bytes 0`, `drak-status 0`, `drak-ack-bytes 0`, `drak-ack-checksum 0x00000000`, `drak-ack-cap 0xFFFFFFFF`, `drak-block-endpoint 0`, `drak-block-cap 0`, `drak-fs-minted 0`, `drak-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drak-flags 0x7FFFFC01`.

The read-close checkpoint consumes that denied read-ack token only as a no-success-close denial. UEFI removable and ISO media prove `drcl-state 3`, `drcl-flags 0x3FFFFFFF`, read-ack token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drcl-read-bytes 2048`, `drcl-page-bytes 4096`, checksum `0x76EFDDC5`, `drcl-drak-denied 1`, `drcl-requested 1`, `drcl-granted 0`, `drcl-denied 1`, `drcl-bytes 0`, `drcl-status 0`, `drcl-close-bytes 0`, `drcl-close-checksum 0x00000000`, `drcl-close-cap 0xFFFFFFFF`, `drcl-block-endpoint 0`, `drcl-block-cap 0`, `drcl-fs-minted 0`, `drcl-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drcl-flags 0x7FFFFC01`.

The read-seal checkpoint consumes that denied read-close token only as a no-success-seal denial. UEFI removable and ISO media prove `drsl-state 3`, `drsl-flags 0x3FFFFFFF`, read-close token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drsl-read-bytes 2048`, `drsl-page-bytes 4096`, checksum `0x76EFDDC5`, `drsl-drcl-denied 1`, `drsl-requested 1`, `drsl-granted 0`, `drsl-denied 1`, `drsl-bytes 0`, `drsl-status 0`, `drsl-seal-bytes 0`, `drsl-seal-checksum 0x00000000`, `drsl-seal-cap 0xFFFFFFFF`, `drsl-block-endpoint 0`, `drsl-block-cap 0`, `drsl-fs-minted 0`, `drsl-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drsl-flags 0x7FFFFC01`.

The read-discard checkpoint consumes that denied read-unseal token only as a no-success-discard denial. UEFI removable and ISO media prove `drdc-state 3`, `drdc-flags 0x3FFFFFFF`, read-unseal token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drdc-read-bytes 2048`, `drdc-page-bytes 4096`, checksum `0x76EFDDC5`, `drdc-drul-denied 1`, `drdc-requested 1`, `drdc-granted 0`, `drdc-denied 1`, `drdc-bytes 0`, `drdc-status 0`, `drdc-discard-bytes 0`, `drdc-discard-checksum 0x00000000`, `drdc-discard-cap 0xFFFFFFFF`, `drdc-block-endpoint 0`, `drdc-block-cap 0`, `drdc-fs-minted 0`, `drdc-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drdc-flags 0x7FFFFC01`.

The read-finalize checkpoint consumes that denied read-discard token only as a no-success-finalize denial. UEFI removable and ISO media prove `driver-read-finalize-state 3`, `driver-read-finalize-flags 0x3FFFFFFF`, read-discard token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-read-finalize-read-bytes 2048`, `driver-read-finalize-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-read-finalize-read-discard-denied 1`, `driver-read-finalize-requested 1`, `driver-read-finalize-granted 0`, `driver-read-finalize-denied 1`, `driver-read-finalize-bytes-available 0`, `driver-read-finalize-status 0`, `driver-read-finalize-finalized-bytes 0`, `driver-read-finalize-finalize-checksum 0x00000000`, `driver-read-finalize-finalize-cap 0xFFFFFFFF`, `driver-read-finalize-block-endpoint 0`, `driver-read-finalize-block-cap-minted 0`, `driver-read-finalize-fs-minted 0`, `driver-read-finalize-buffer-unchanged 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `driver-read-finalize-flags 0x7FFFFC01`.

The read-authorize checkpoint consumes that denied read-finalize token only as a no-success policy authorization denial. UEFI removable and ISO media prove `driver-read-authorize-state 3`, `driver-read-authorize-flags 0x3FFFFFFF`, read-finalize token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-read-authorize-read-bytes 2048`, `driver-read-authorize-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-read-authorize-read-finalize-denied 1`, `driver-read-authorize-requested 1`, `driver-read-authorize-granted 0`, `driver-read-authorize-denied 1`, `driver-read-authorize-policy-grant 0`, `driver-read-authorize-issue-authority 0`, `driver-read-authorize-dma-authority 0`, `driver-read-authorize-media-read-authority 0`, `driver-read-authorize-write-authority 0`, `driver-read-authorize-commit-authority 0`, `driver-read-authorize-block-endpoint 0`, `driver-read-authorize-block-cap-minted 0`, `driver-read-authorize-fs-minted 0`, `driver-read-authorize-buffer-unchanged 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `driver-read-authorize-flags 0x7FFFFC01`.

The read-dispatch checkpoint consumes that denied read-authorize token only as a no-success driver dispatch/queue denial. UEFI removable and ISO media prove `driver-read-dispatch-state 3`, `driver-read-dispatch-flags 0x3FFFFFFF`, read-authorize token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-read-dispatch-read-bytes 2048`, `driver-read-dispatch-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-read-dispatch-read-authorize-denied 1`, `driver-read-dispatch-requested 1`, `driver-read-dispatch-granted 0`, `driver-read-dispatch-denied 1`, `driver-read-dispatch-policy-grant 0`, `driver-read-dispatch-dispatch-queued 0`, `driver-read-dispatch-queue-depth 0`, `driver-read-dispatch-issue-authority 0`, `driver-read-dispatch-dma-authority 0`, `driver-read-dispatch-media-read-authority 0`, `driver-read-dispatch-write-authority 0`, `driver-read-dispatch-commit-authority 0`, `driver-read-dispatch-block-endpoint 0`, `driver-read-dispatch-block-cap-minted 0`, `driver-read-dispatch-fs-minted 0`, `driver-read-dispatch-buffer-unchanged 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `driver-read-dispatch-flags 0x7FFFFC01`.

The read-queue checkpoint consumes that denied read-dispatch token only as a no-success queue-admission denial. UEFI removable and ISO media prove `driver-read-queue-state 3`, `driver-read-queue-flags 0x3FFFFFFF`, read-dispatch token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-read-queue-read-bytes 2048`, `driver-read-queue-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-read-queue-read-dispatch-denied 1`, `driver-read-queue-requested 1`, `driver-read-queue-granted 0`, `driver-read-queue-denied 1`, `driver-read-queue-policy-grant 0`, `driver-read-queue-queue-inserted 0`, `driver-read-queue-queue-depth 0`, `driver-read-queue-worker-wake 0`, `driver-read-queue-issue-authority 0`, `driver-read-queue-dma-authority 0`, `driver-read-queue-media-read-authority 0`, `driver-read-queue-write-authority 0`, `driver-read-queue-commit-authority 0`, `driver-read-queue-block-endpoint 0`, `driver-read-queue-block-cap-minted 0`, `driver-read-queue-fs-minted 0`, `driver-read-queue-buffer-unchanged 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `driver-read-queue-flags 0x7FFFFC01`.

The read-worker checkpoint consumes that denied read-queue token only as a no-success worker-admission denial. UEFI removable and ISO media prove `driver-read-worker-state 3`, `driver-read-worker-flags 0x3FFFFFFF`, read-queue token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-read-worker-read-bytes 2048`, `driver-read-worker-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-read-worker-read-queue-denied 1`, `driver-read-worker-requested 1`, `driver-read-worker-granted 0`, `driver-read-worker-denied 1`, `driver-read-worker-policy-grant 0`, `driver-read-worker-queue-inserted 0`, `driver-read-worker-queue-depth 0`, `driver-read-worker-worker-wake 0`, `driver-read-worker-worker-dequeued 0`, `driver-read-worker-issue-authority 0`, `driver-read-worker-dma-authority 0`, `driver-read-worker-media-read-authority 0`, `driver-read-worker-write-authority 0`, `driver-read-worker-commit-authority 0`, `driver-read-worker-block-endpoint 0`, `driver-read-worker-block-cap-minted 0`, `driver-read-worker-fs-minted 0`, `driver-read-worker-buffer-unchanged 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `driver-read-worker-flags 0x7FFFFC01`.

The read-schedule checkpoint consumes that denied read-worker token only as a no-success scheduler/run-queue admission denial. UEFI removable and ISO media prove `driver-read-schedule-state 3`, `driver-read-schedule-flags 0x3FFFFFFF`, read-worker token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-read-schedule-read-bytes 2048`, `driver-read-schedule-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-read-schedule-read-worker-denied 1`, `driver-read-schedule-requested 1`, `driver-read-schedule-granted 0`, `driver-read-schedule-denied 1`, `driver-read-schedule-policy-grant 0`, `driver-read-schedule-queue-inserted 0`, `driver-read-schedule-queue-depth 0`, `driver-read-schedule-worker-wake 0`, `driver-read-schedule-worker-dequeued 0`, `driver-read-schedule-worker-runnable 0`, `driver-read-schedule-worker-scheduled 0`, `driver-read-schedule-issue-authority 0`, `driver-read-schedule-dma-authority 0`, `driver-read-schedule-media-read-authority 0`, `driver-read-schedule-write-authority 0`, `driver-read-schedule-commit-authority 0`, `driver-read-schedule-block-endpoint 0`, `driver-read-schedule-block-cap-minted 0`, `driver-read-schedule-fs-minted 0`, `driver-read-schedule-buffer-unchanged 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `driver-read-schedule-flags 0x7FFFFC01`.

The read-run checkpoint consumes that denied read-schedule token only as a no-success block-worker run-entry denial. UEFI removable and ISO media prove `driver-read-run-state 3`, `driver-read-run-flags 0x3FFFFFFF`, read-schedule token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-read-run-read-bytes 2048`, `driver-read-run-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-read-run-read-schedule-denied 1`, `driver-read-run-requested 1`, `driver-read-run-granted 0`, `driver-read-run-denied 1`, `driver-read-run-policy-grant 0`, `driver-read-run-queue-inserted 0`, `driver-read-run-queue-depth 0`, `driver-read-run-worker-wake 0`, `driver-read-run-worker-dequeued 0`, `driver-read-run-worker-runnable 0`, `driver-read-run-worker-scheduled 0`, `driver-read-run-worker-run 0`, `driver-read-run-worker-executed 0`, `driver-read-run-issue-authority 0`, `driver-read-run-dma-authority 0`, `driver-read-run-media-read-authority 0`, `driver-read-run-write-authority 0`, `driver-read-run-commit-authority 0`, `driver-read-run-block-endpoint 0`, `driver-read-run-block-cap-minted 0`, `driver-read-run-fs-minted 0`, `driver-read-run-buffer-unchanged 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `driver-read-run-flags 0x7FFFFC01`.

The read-body checkpoint consumes that denied read-run token only as a no-success block-worker body-entry denial. UEFI removable and ISO media prove `driver-read-body-state 3`, `driver-read-body-flags 0x3FFFFFFF`, read-run token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-read-body-read-bytes 2048`, `driver-read-body-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-read-body-read-run-denied 1`, `driver-read-body-requested 1`, `driver-read-body-granted 0`, `driver-read-body-denied 1`, `driver-read-body-policy-grant 0`, `driver-read-body-queue-inserted 0`, `driver-read-body-queue-depth 0`, `driver-read-body-worker-wake 0`, `driver-read-body-worker-dequeued 0`, `driver-read-body-worker-runnable 0`, `driver-read-body-worker-scheduled 0`, `driver-read-body-worker-run 0`, `driver-read-body-worker-executed 0`, `driver-read-body-body-entered 0`, `driver-read-body-body-completed 0`, `driver-read-body-issue-authority 0`, `driver-read-body-dma-authority 0`, `driver-read-body-media-read-authority 0`, `driver-read-body-write-authority 0`, `driver-read-body-commit-authority 0`, `driver-read-body-block-endpoint 0`, `driver-read-body-block-cap-minted 0`, `driver-read-body-fs-minted 0`, `driver-read-body-buffer-unchanged 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `driver-read-body-flags 0x7FFFFC01`.

The read-issue checkpoint consumes that denied read-body token only as a no-success command-issue entry denial. UEFI removable and ISO media prove `driver-read-issue-state 3`, `driver-read-issue-flags 0x3FFFFFFF`, read-body token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-read-issue-read-bytes 2048`, `driver-read-issue-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-read-issue-read-body-denied 1`, `driver-read-issue-requested 1`, `driver-read-issue-granted 0`, `driver-read-issue-denied 1`, `driver-read-issue-policy-grant 0`, `driver-read-issue-queue-inserted 0`, `driver-read-issue-queue-depth 0`, `driver-read-issue-worker-wake 0`, `driver-read-issue-worker-dequeued 0`, `driver-read-issue-worker-runnable 0`, `driver-read-issue-worker-scheduled 0`, `driver-read-issue-worker-run 0`, `driver-read-issue-worker-executed 0`, `driver-read-issue-issue-entered 0`, `driver-read-issue-issue-completed 0`, `driver-read-issue-issue-authority 0`, `driver-read-issue-dma-authority 0`, `driver-read-issue-media-read-authority 0`, `driver-read-issue-write-authority 0`, `driver-read-issue-commit-authority 0`, `driver-read-issue-block-endpoint 0`, `driver-read-issue-block-cap-minted 0`, `driver-read-issue-fs-minted 0`, `driver-read-issue-buffer-unchanged 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `driver-read-issue-flags 0x7FFFFC01`.

The read-DMA checkpoint consumes that denied read-issue token only as a DMA-window denial. UEFI removable and ISO media prove `driver-read-dma-state 3`, `driver-read-dma-flags 0x3FFFFFFF`, read-issue token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `driver-read-dma-read-bytes 2048`, `driver-read-dma-page-bytes 4096`, checksum `0x76EFDDC5`, `driver-read-dma-read-issue-denied 1`, `driver-read-dma-requested 1`, `driver-read-dma-granted 0`, `driver-read-dma-denied 1`, `driver-read-dma-policy-grant 0`, `driver-read-dma-bytes-available 0`, `driver-read-dma-window-cap 0xFFFFFFFF`, `driver-read-dma-window-open 0`, `driver-read-dma-entered 0`, `driver-read-dma-completed 0`, `driver-read-dma-issue-authority 0`, `driver-read-dma-dma-authority 0`, `driver-read-dma-media-read-authority 0`, `driver-read-dma-write-authority 0`, `driver-read-dma-commit-authority 0`, `driver-read-dma-block-endpoint 0`, `driver-read-dma-block-cap-minted 0`, `driver-read-dma-fs-minted 0`, `driver-read-dma-buffer-unchanged 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `driver-read-dma-flags 0x7FFFFC01`.

The read-IRQ checkpoint consumes that denied read-DMA token only as an IRQ/completion observation denial, now using compact `drs-irq` proof labels. UEFI removable and ISO media prove `drs-irq-state 3`, `drs-irq-flags 0x3FFFFFFF`, read-DMA token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drs-irq-read-bytes 2048`, `drs-irq-page-bytes 4096`, checksum `0x76EFDDC5`, `drs-irq-dma-denied 1`, requested/granted/denied `1/0/1`, `drs-irq-policy-grant 0`, `drs-irq-bytes 0`, `drs-irq-wait 0`, `drs-irq-fired 0`, result status/bytes/checksum all zero, issue/DMA/read/write/commit authority all zero, no block endpoint/cap/filesystem mint, `drs-irq-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drs-irq-flags 0x7FFFFC01`.

The read-status checkpoint consumes that denied read-IRQ token only as a status/completion polling denial, now using compact `drs-status` proof labels. UEFI removable and ISO media prove `drs-status-state 3`, `drs-status-flags 0x3FFFFFFF`, read-IRQ token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drs-status-read-bytes 2048`, `drs-status-page-bytes 4096`, checksum `0x76EFDDC5`, `drs-status-irq-denied 1`, requested/granted/denied `1/0/1`, `drs-status-policy-grant 0`, `drs-status-bytes 0`, `drs-status-poll 0`, `drs-status-sready 0`, `drs-status-pxis 0x00000000`, `drs-status-ci 0x00000000`, `drs-status-tfd 0x00000000`, `drs-status-serr 0x00000000`, `drs-status-irq-clear 0`, result status/bytes/checksum all zero, issue/DMA/read/write/commit authority all zero, no block endpoint/cap/filesystem mint, `drs-status-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drs-status-flags 0x7FFFFC01`.

The read-status-result checkpoint consumes that denied read-status token only as a read-result denial, now using compact `drs-result` proof labels. UEFI removable and ISO media prove `drs-result-state 3`, `drs-result-flags 0x3FFFFFFF`, read-status token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drs-result-read-bytes 2048`, `drs-result-page-bytes 4096`, checksum `0x76EFDDC5`, `drs-result-status-denied 1`, requested/granted/denied `1/0/1`, `drs-result-policy-grant 0`, `drs-result-bytes 0`, result status/bytes/checksum all zero, issue/DMA/read/write/commit authority all zero, no block endpoint/cap/filesystem mint, `drs-result-buffer 1`, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drs-result-flags 0x7FFFFC01`.

The read-status-sample checkpoint consumes that denied read-status-result token only for a driver-owned, query-only, read-only AHCI port status sample, now using compact `drs-sample` proof labels. UEFI removable and ISO media prove `drs-sample-state 3`, `drs-sample-flags 0x3FFFFFFF`, read-status-result token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drs-sample-read-bytes 2048`, `drs-sample-page-bytes 4096`, checksum `0x76EFDDC5`, nonzero hardware-backed `drs-sample-pxis`, `drs-sample-ci 0x00000000`, `drs-sample-tfd 0x00000050`, `drs-sample-serr 0x00000000`, `drs-sample-tfd-ready 1`, `drs-sample-ci-idle 1`, `drs-sample-serr-clear 1`, `drs-sample-irq-clear 0`, result status/bytes/checksum all zero, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drs-sample-flags 0x7FF00C01`.

The read-status-clear checkpoint consumes that status-sample token only as a denied PxIS clear request under the same driver-owned query-only capability, now using compact `drs-clear` proof labels. UEFI removable and ISO media prove `drs-clear-state 3`, `drs-clear-flags 0x3FFFFFFF`, status-sample token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drs-clear-read-bytes 2048`, `drs-clear-page-bytes 4096`, checksum `0x76EFDDC5`, sample-ready/bound `1`, `drs-clear-pxis-b 0x00000003`, `drs-clear-pxis-a 0x00000003`, `drs-clear-pxis-same 1`, `drs-clear-ci 0x00000000`, `drs-clear-tfd 0x00000050`, `drs-clear-serr 0x00000000`, `drs-clear-clear-requested 1`, `drs-clear-clear-granted 0`, `drs-clear-clear-denied 1`, `drs-clear-clear-value 0x00000000`, `drs-clear-irq-clear 0`, result status/bytes/checksum all zero, no block endpoint/cap/filesystem mint, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drs-clear-flags 0x7FF03801`.

The read-status-clear-result checkpoint consumes that denied status-clear token only to prove no clear result can be surfaced, now using compact `drs-clear-result` proof labels. UEFI removable and ISO media prove `drs-clear-result-state 3`, `drs-clear-result-flags 0x3FFFFFFF`, status-clear token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drs-clear-result-read-bytes 2048`, `drs-clear-result-page-bytes 4096`, checksum `0x76EFDDC5`, copied PxIS before/after `0x00000003`, `drs-clear-result-pxis-same 1`, `drs-clear-result-result-requested 1`, `drs-clear-result-result-granted 0`, `drs-clear-result-result-denied 1`, `drs-clear-result-result-status 0`, `drs-clear-result-result-bytes 0`, `drs-clear-result-result-checksum 0x00000000`, no block endpoint/cap/filesystem mint, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drs-clear-result-flags 0x7FF0E401`.

The read-status-resample checkpoint consumes that denied clear-result token only to reread PxIS/PxCI/PxTFD/PxSERR through the driver-owned query-only hardware capability, now using compact `drs-resample` proof labels. UEFI removable and ISO media prove `drs-resample-state 3`, `drs-resample-flags 0x3FFFFFFF`, clear-result-token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drs-resample-read-bytes 2048`, `drs-resample-page-bytes 4096`, checksum `0x76EFDDC5`, copied PxIS before/after `0x00000003`, `drs-resample-pxis-same 1`, `drs-resample-ci 0x00000000`, `drs-resample-tfd 0x00000050`, `drs-resample-serr 0x00000000`, result status/bytes/checksum all zero, no block endpoint/cap/filesystem mint, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drs-resample-flags 0x7FC01C01`.

The read-status-stable checkpoint consumes the resample token only to seal that the sampled status remained stable across the earlier read-only observations, now using compact `drs-stable` proof labels. UEFI removable and ISO media prove `drs-stable-state 3`, `drs-stable-flags 0x3FFFFFFF`, resample-token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, read shape/checksum `2048`/`4096`/`0x76EFDDC5`, stable PxIS before/after, CI `0x00000000`, TFD `0x00000050`, SERR `0x00000000`, result status/bytes/checksum all zero, no block endpoint/cap/filesystem mint, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drs-stable-flags 0x7FE03401`.

The read-status-guard checkpoint consumes that stable-status token only to bind a future read-command shape behind explicit denials, now using compact `drs-guard` proof labels. UEFI removable and ISO media prove `drs-guard-state 3`, `drs-guard-flags 0x3FFFFFFF`, stable-token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drs-guard-read-bytes 2048`, `drs-guard-page-bytes 4096`, checksum `0x76EFDDC5`, stable PxIS/CI/TFD/SERR preconditions, `drs-guard-issue-denied 1`, `drs-guard-dma-denied 1`, `drs-guard-read-denied 1`, `drs-guard-write-denied 1`, `drs-guard-commit-denied 1`, result status/bytes/checksum all zero, no block endpoint/cap/filesystem mint, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drs-guard-flags 0x7FFC0001`.

The read-status-buffer checkpoint consumes that guard token only to make the future result-buffer surface auditable and sealed, now using compact `drs-buffer` proof labels. UEFI removable and ISO media prove `drs-buffer-state 3`, `drs-buffer-flags 0x3FFFFFFF`, guard-token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drs-buffer-read-bytes 2048`, `drs-buffer-page-bytes 4096`, checksum `0x76EFDDC5`, `drs-buffer-view-requested 1`, `drs-buffer-view-granted 0`, `drs-buffer-view-denied 1`, result status/bytes/checksum all zero, read/write/commit authority all zero, no block endpoint/cap/filesystem mint, and continued zero MMIO-write/port-program/publish/command/DMA/arm/media-read/media-write side effects; BIOS/no-AHCI media reports unavailable `drs-buffer-flags 0x7FFFF801`.

The read-status-export checkpoint consumes that sealed status-buffer token only as an explicit caller-visible export denial, now using compact `drs-export` proof labels. UEFI removable and ISO media prove `drs-export-state 3`, `drs-export-flags 0x3FFFFFFF`, status-buffer-token binding, owner `0x00001006`, query-only binding, selected ATAPI port, operation `2`, LBA `0`, one block, `drs-export-read-bytes 2048`, checksum `0x76EFDDC5`, `drs-export-sealed 1`, `drs-export-requested 1`, `drs-export-granted 0`, `drs-export-denied 1`, `drs-export-user-copy 0x00000000`, `drs-export-authority 0x00000000`, `drs-export-effects 0x00000000`, and `drs-export-buffer 1`; BIOS/no-AHCI media reports unavailable `drs-export-flags 0x7FFFF001`.

The read-status-report checkpoint consumes that denied export token only to prove no broker/report-facing read result can appear under query-only authority, now using compact `drs-report` proof labels. UEFI removable and ISO media prove `drs-report-state 3`, `drs-report-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-report-export-denied 1`, report/user-copy/authority/effect masks all `0x00000000`, and `drs-report-buffer 1`; BIOS/no-AHCI media reports unavailable `drs-report-flags 0x7FFFFE01`.

The read-status-receipt checkpoint consumes that denied report token only to prove no caller-visible success receipt can appear under query-only authority while using compact `drs-receipt` proof labels. UEFI removable and ISO media prove `drs-receipt-state 3`, `drs-receipt-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-receipt-report-denied 1`, receipt/user-copy/authority/effect masks all `0x00000000`, and `drs-receipt-buffer 1`; BIOS/no-AHCI media reports unavailable `drs-receipt-flags 0x7FFFFF01`.

The read-status-ack checkpoint consumes that denied receipt token only to prove no success acknowledgment, copied bytes, storage authority, or side effect can appear under query-only authority while using compact `drs-ack` proof labels. UEFI removable and ISO media prove `drs-ack-state 3`, `drs-ack-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-ack-receipt-denied 1`, ack/user-copy/authority/effect masks all `0x00000000`, and `drs-ack-buffer 1`; BIOS/no-AHCI media reports unavailable `drs-ack-flags 0x7FFFFF01`.

The read-status-close checkpoint consumes that denied acknowledgment token only to prove no close status, close bytes, close checksum, close capability, copied bytes, storage authority, or side effect can appear under query-only authority while using compact `drs-close` proof labels. UEFI removable and ISO media prove `drs-close-state 3`, `drs-close-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-close-ack-denied 1`, close/user-copy/authority/effect masks all `0x00000000`, and `drs-close-buffer 1`; BIOS/no-AHCI media reports unavailable `drs-close-flags 0x7FFFFF01`.

The read-status-seal checkpoint consumes that denied close token only to prove no seal status, sealed bytes, seal checksum, seal capability, copied bytes, storage authority, or side effect can appear under query-only authority while using compact `drs-seal` proof labels. UEFI removable and ISO media prove `drs-seal-state 3`, `drs-seal-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-seal-close-denied 1`, seal/user-copy/authority/effect masks all `0x00000000`, and `drs-seal-buffer 1`; BIOS/no-AHCI media reports unavailable `drs-seal-flags 0x7FFFFF01`.

The read-status-unseal checkpoint consumes that denied seal token only to prove no unseal status, unsealed bytes, unseal checksum, unseal capability, copied bytes, storage authority, or side effect can appear under query-only authority while using compact `drs-unseal` proof labels. UEFI removable and ISO media prove `drs-unseal-state 3`, `drs-unseal-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-unseal-seal-denied 1`, unseal/user-copy/authority/effect masks all `0x00000000`, and `drs-unseal-buffer 1`; BIOS/no-AHCI media reports unavailable `drs-unseal-flags 0x7FFFFF01`.

The read-status-discard checkpoint consumes that denied unseal token only to prove no discard status, discarded bytes, discard checksum, discard capability, copied bytes, storage authority, or side effect can appear under query-only authority while using compact `drs-discard` proof labels. UEFI removable and ISO media prove `drs-discard-state 3`, `drs-discard-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-discard-unseal-denied 1`, discard/user-copy/authority/effect masks all `0x00000000`, and `drs-discard-buffer 1`; BIOS/no-AHCI media reports unavailable `drs-discard-flags 0x7FFFFF01`.

The newest read-status-finalize checkpoint consumes that denied discard token only to prove no finalize status, finalized bytes, finalize checksum, finalize capability, copied bytes, storage authority, or side effect can appear under query-only authority while using compact `drs-final` proof labels to preserve BIOS loader headroom. UEFI removable and ISO media prove `drs-final-state 3`, `drs-final-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-final-discard-denied 1`, finish/user-copy/authority/effect masks all `0x00000000`, and `drs-final-buffer 1`; BIOS/no-AHCI media reports unavailable `drs-final-flags 0x7FFFFF01`.

The read-status-authorize checkpoint consumes that denied finalize token only to prove no authorization grant, issue authority, DMA authority, media-read authority, copied bytes, storage authority, or side effect can appear under query-only authority. UEFI removable and ISO media prove compact `drs-authz-state 3`, `drs-authz-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-authz-final-denied 1`, grant/user-copy/authority/effect masks all `0x00000000`, and `drs-authz-buffer 1`; BIOS/no-AHCI media reports unavailable `drs-authz-flags 0x7FFFFF01`.

The newest read-status-dispatch checkpoint consumes that denied authorize token only to prove no dispatch handle, queue entry, worker admission, scheduling, command issue, DMA, media read/write, copied bytes, storage authority, or side effect can appear under query-only authority. UEFI removable and ISO media prove `drs-dispatch-state 3`, `drs-dispatch-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-dispatch-authz-denied 1`, aggregate safety mask `drs-dispatch-safety 0x00000000`, and `drs-dispatch-buffer 1`; BIOS/no-AHCI media reports unavailable `drs-dispatch-flags 0x7FFFFF01`.

The read-status-queue checkpoint consumes that denied dispatch token only to prove no queue ticket, queue entry, queue depth, worker admission, worker wake, worker runnable state, worker schedule, issue authority, DMA authority, media-read authority, write/commit authority, copied bytes, storage authority, or side effect can appear under query-only authority. UEFI removable and ISO media prove `drs-queue-state 3`, `drs-queue-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-queue-dispatch-denied 1`, safety mask `drs-queue-safety 0x00000000`, queue depth/admit/worker/runnable/schedule all `0`, and `drs-queue-buffer 1`; BIOS/no-AHCI media reports unavailable `drs-queue-flags 0x7FFFFF01`.

The read-status-worker checkpoint consumes that denied queue token only to prove no queue dequeue, worker admission, worker wake, runnable worker, worker schedule, worker run, worker execution, issue authority, DMA authority, media-read authority, write/commit authority, copied bytes, storage authority, or side effect can appear under query-only authority. UEFI removable and ISO media prove `drs-w-state 3`, `drs-w-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-w-queue-denied 1`, safety mask `drs-w-safety 0x00000000`, explicit zero worker telemetry `drs-w-dequeue 0`, `drs-w-admit 0`, `drs-w-wake 0`, `drs-w-runnable 0`, `drs-w-sched 0`, `drs-w-run 0`, `drs-w-exec 0`, and `drs-w-buffer 1`; BIOS/no-AHCI media reports unavailable `drs-w-flags 0x7FFFFF01` with the same zero worker telemetry.

The read-status read-authority checkpoint consumes the denied worker token and mints the first sealed, verifier-backed hardware read-authority primitive for the block-worker principal without opening execution authority. UEFI removable and ISO media prove `drs-rauth-state 3`, `drs-rauth-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-rauth-worker-denied 1`, `drs-rauth-policy 1`, `drs-rauth-read 1`, and explicit zero issue, DMA, media-read, write, commit, block-endpoint, block-capability, filesystem-mint, queue-dequeue, worker-admit, worker-wake, worker-runnable, worker-schedule, worker-run, and worker-execute telemetry. BIOS/no-AHCI media reports unavailable `drs-rauth-flags 0x7FFFF901`, no read authority, no staged token, and no hardware side effects. This is intentionally a sealed authority transition, not a media-byte read, AHCI command issue, DMA mapping, controller-register write, block endpoint publication, filesystem grant, or persistent write path.

The read-status descriptor checkpoint consumes that sealed `drs-rauth` token into the first driver-owned, non-executing AHCI/ATAPI read descriptor shape. UEFI removable and ISO media prove `drs-desc-state 3`, `drs-desc-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-desc-rauth 1`, `drs-desc-shaped 1`, `drs-desc-read 1`, selected ATAPI port, operation `2`, LBA `0`, one block, `2048` read bytes, `4096` page bytes, slot `0`, header `32`, command table `144`, CFIS `20`, PRDT `1`/`16`, ATAPI packet `12`, command opcode `0xA0`, packet opcode `0x28`, transfer `2048`, and zero issue, DMA, media-read, write, commit, block-endpoint, block-capability, filesystem-mint, queue-dequeue, worker-admit, worker-wake, worker-runnable, worker-schedule, worker-run, worker-execute, MMIO-write, port-program, command, DMA, and media-read authority. BIOS/no-AHCI media reports unavailable `drs-desc-flags 0x7FFFF901`. This descriptor is still a proof object, not an AHCI command issue, DMA map, controller write, media-byte read, block endpoint, filesystem grant, or persistent write path.

The read-status command-table checkpoint consumes that sealed `drs-desc` token into a driver-owned AHCI/ATAPI command-table materialization proof without issuing it. UEFI removable and ISO media prove `drs-ctab-state 3`, `drs-ctab-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-ctab-desc 1`, `drs-ctab-mat 1`, `drs-ctab-ready 1`, selected ATAPI port, kind/operation `2`, LBA `0`, one block, `2048` read bytes, `4096` page bytes, checksum transition `0x76EFDDC5 -> 0x3FBFAF45`, header flags `0x00010025`, CFIS command `0xA0`, ATAPI packet opcode `0x28`, PRDT DBC `2047`, `drs-ctab-written 1`, and zero issue, DMA, media-read, write, commit, block-endpoint, block-capability, filesystem-mint, MMIO-write, port-program, command, DMA, and media-read authority. BIOS/no-AHCI media reports unavailable `drs-ctab-flags 0x7FFFF901`. This is still a private table-preparation proof, not command issue, DMA mapping, controller-register programming, media-byte read, block endpoint publication, filesystem grant, or persistent write path.

The read-status command-issue checkpoint consumes that sealed `drs-ctab` token into the first driver-owned AHCI/ATAPI command-issue boundary proof while still denying issue authority. UEFI removable and ISO media prove `drs-issue-state 3`, `drs-issue-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-issue-ctab 1`, `drs-issue-ready 1`, `drs-issue-request 1`, `drs-issue-grant 0`, `drs-issue-denied 1`, selected ATAPI port, kind/operation `2`, LBA `0`, one block, `2048` read bytes, `4096` page bytes, `drs-issue-ci 0`, `drs-issue-mask 1`, idle slot, ready TFD/SERR policy, command-table checksum `0x3FBFAF45`, checksum match, and zero issue, DMA, media-read, write, commit, block-endpoint, block-capability, filesystem-mint, MMIO-write, port-program, command, DMA, and media-read authority. BIOS/no-AHCI media reports unavailable `drs-issue-flags 0x7FFFF901`. This is still a denied issue-boundary proof, not command issue, DMA mapping, controller-register programming, media-byte read, block endpoint publication, filesystem grant, or persistent write path.

The read-status issue-grant checkpoint consumes that sealed `drs-issue` token into a separate driver-owned issue-grant policy proof while still denying issue authority. UEFI removable and ISO media prove `drs-grant-state 3`, `drs-grant-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-grant-issue 1`, `drs-grant-ready 1`, `drs-grant-request 1`, `drs-grant-grant 0`, `drs-grant-denied 1`, selected ATAPI port, kind/operation `2`, LBA `0`, one block, `2048` read bytes, `4096` page bytes, `drs-grant-ci 0`, `drs-grant-mask 1`, idle slot, ready TFD/SERR policy, command-table checksum `0x3FBFAF45`, checksum match, and zero issue, DMA, media-read, write, commit, block-endpoint, block-capability, filesystem-mint, MMIO-write, port-program, command, DMA, and media-read authority. BIOS/no-AHCI media reports unavailable `drs-grant-flags 0x7FFFF901`. This is still a denied grant-boundary proof, not command issue, DMA mapping, controller-register programming, media-byte read, block endpoint publication, filesystem grant, or persistent write path.

The read-status arm checkpoint consumes that sealed `drs-grant` token into a driver-owned arm/doorbell preflight proof while still denying arm and issue authority. UEFI removable and ISO media prove `drs-arm-state 3`, `drs-arm-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-arm-grant 1`, `drs-arm-ready 1`, `drs-arm-request 1`, `drs-arm-arm 0`, `drs-arm-denied 1`, selected ATAPI port, kind/operation `2`, LBA `0`, one block, `2048` read bytes, `4096` page bytes, `drs-arm-ci 0`, `drs-arm-mask 1`, idle slot, ready TFD/SERR policy, command-table checksum `0x3FBFAF45`, checksum match, and zero issue, DMA, media-read, write, commit, block-endpoint, block-capability, filesystem-mint, MMIO-write, port-program, command, DMA, and media-read authority. BIOS/no-AHCI media reports unavailable `drs-arm-flags 0x7FFFF901`. This is still a denied arm-boundary proof, not doorbell/MMIO execution, command issue, DMA mapping, controller-register programming, media-byte read, block endpoint publication, filesystem grant, or persistent write path.

The read-status execution checkpoint consumes that sealed `drs-arm` token into a driver-owned execution/doorbell preflight proof while still denying execution. UEFI removable and ISO media prove `drs-exec-state 3`, `drs-exec-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-exec-arm 1`, `drs-exec-ready 1`, `drs-exec-request 1`, `drs-exec-exec 0`, `drs-exec-denied 1`, selected ATAPI port, kind/operation `2`, LBA `0`, one block, `2048` read bytes, `4096` page bytes, `drs-exec-ci 0`, `drs-exec-mask 1`, idle slot, ready TFD/SERR policy, command-table checksum `0x3FBFAF45`, checksum match, and zero issue, DMA, media-read, write, commit, block-endpoint, block-capability, filesystem-mint, MMIO-write, port-program, command, DMA, and media-read authority. BIOS/no-AHCI media reports unavailable `drs-exec-flags 0x7FFFF901`. This is still a denied execution-boundary proof, not a real AHCI doorbell, command issue, DMA mapping, controller-register programming, media-byte read, block endpoint publication, filesystem grant, or persistent write path.

The read-status DMA checkpoint consumes that sealed `drs-exec` token into a driver-owned DMA-window preflight proof while still denying DMA mapping. UEFI removable and ISO media prove `drs-dma-state 3`, `drs-dma-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, `drs-dma-exec 1`, `drs-dma-ready 1`, `drs-dma-request 1`, `drs-dma-grant 0`, `drs-dma-denied 1`, selected ATAPI port, kind/operation `2`, LBA `0`, one block, `2048` read bytes, `4096` page bytes, `drs-dma-ci 0`, `drs-dma-mask 1`, idle slot, ready TFD/SERR policy, command-table checksum `0x3FBFAF45`, checksum match, and zero issue, DMA, media-read, write, commit, block-endpoint, block-capability, filesystem-mint, MMIO-write, port-program, command, DMA, and media-read authority. BIOS/no-AHCI media reports unavailable `drs-dma-flags 0x7FFFF901`. This is still a denied DMA-boundary proof, not a DMA map, command issue, AHCI doorbell, controller-register programming, media-byte read, block endpoint publication, filesystem grant, or persistent write path.

The newest read-status MMIO side-effect checkpoint consumes that denied `drs-dma` token into a broker-owned, single-register write aperture without command issue or DMA. UEFI removable and ISO media prove `drs-mmio-state 3`, `drs-mmio-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, DMA-token binding, the exact selected-port `PxIS` register offset (`0x00000210` for UEFI removable media and `0x00000390` for ISO in QEMU), exact write value `0x00000000`, PxIS before/after `0x00000003` unchanged, rollback-required `0`, teardown `1`, stale-token denial `1`, `drs-mmio-mmio 1`, and zero issue authority, DMA authority, media-read authority, write/commit authority, block endpoint/capability, filesystem mint, port-programming, command issue, DMA, media-read, or media-write side effects. BIOS/no-AHCI media reports unavailable `drs-mmio-flags 0x7FF00101`. This is the first real AHCI MMIO store, but it is a zero-value status-register no-op behind a temporary single-page write window that is closed before readback; it is not a doorbell, command issue, DMA map, media-byte read, block endpoint publication, filesystem grant, interrupt-clear completion, or persistent write path.

The read-status DMA-window checkpoint consumes the `drs-mmio` token into a broker-owned, single-page bounce-window proof that opens and closes before any AHCI command can run. UEFI removable and ISO media prove `drs-dwin-state 3`, `drs-dwin-flags 0x3FFFFFFF`, block-worker owner `0x00001006`, query-only binding, checksum `0x76EFDDC5`, MMIO-token binding, selected ATAPI port, operation `2`, LBA `0`, one block, `2048` read bytes, `4096` page bytes, command-table checksum match, page/bounce physical addresses, bounce offset `2048`, range end `4096`, single-page, broker-owned, bounds-enforced, confined, below-4G, non-user-mapped, no-uncontrolled-alias, lifetime opened and closed, revocation required/done, stale-token denial, and zero issue authority, DMA execution authority, media-read authority, write/commit authority, block endpoint/capability, filesystem mint, MMIO write, port programming, command issue, DMA, media-read, or media-write side effects. BIOS/no-AHCI media reports unavailable `drs-dwin-flags 0x7FC80001`. This is a real broker-private DMA-window lifetime proof, not device DMA execution, AHCI command issue, doorbell programming, media-byte ingestion, block endpoint publication, filesystem grant, or persistent write path.

The newest `drs-read` checkpoint is the first positive hardware-backed AHCI/ATAPI read. It binds the `drs-dwin` token, patches the broker-private PRDT with the single-page bounce buffer physical address, records the command-table checksum transition `0x14EC2F71 -> 0xD8BD95B6`, opens only the target MMIO page long enough to program `PxCLB/PxCLBU/PxFB/PxFBU`, set `FRE/ST`, clear `PxIS/PxSERR`, issue slot 0 through `PxCI`, poll bounded completion, clear `ST`, close the aperture, and compute a checksum over the sealed bounce buffer. UEFI removable and ISO media prove `drs-read-state 3`, `drs-read-flags 0x007FFFFF`, `drs-read-issued 1`, `drs-read-completed 1`, `drs-read-bytes 2048`, nonzero read checksums, `drs-read-error 0`, `drs-read-prdbc 2048`, CI idle after completion, and zero write/commit/block-endpoint/filesystem-mint authority. BIOS/no-AHCI media remains unavailable at `drs-read-flags 0x801F0001`. The bytes remain broker-private; this is not a block endpoint, filesystem grant, user-visible read result, reusable media lease, or write path.

The `drs-block` checkpoint consumes the sealed `drs-read` token into the first read-only AHCI-backed block route. UEFI removable and ISO media prove `drs-block-state 3`, `drs-block-flags 0x000FFFFF`, block-worker owner `0x00001006`, read-token binding, block endpoint publication, block-cap minting, read-only status, delegated-cap routing, `drs-block-bytes 2048`, checksum equality with `drs-read`, wrong-owner denial, stale delegated-handle denial, and zero write, commit, and filesystem-mint authority. BIOS/no-AHCI media reports unavailable `drs-block-flags 0x800E0001`. This is a read-only block service route over the already-bounded read result, not write authority, format authority, commit authority, or a reusable storage lease.

The `drs-fs` checkpoint consumes that read-only block capability into the first kernel-storage-layer ISO9660 read. UEFI removable verification still boots the removable FAT image, but its AHCI storage proof now uses the generated read-only ISO sidecar so both UEFI and ISO paths validate the same optical filesystem shape. Verification requires `drs-fs-state 3`, `drs-fs-flags 0x0001FFFF`, block-token and block-cap binding, read-only block routing, PVD `CD001` validation, root-directory read, `README.TXT` location, `drs-fs-read-bytes 584`, checksum `0x4ABDFAAA`, content match, and zero write, commit, or caller-delegated filesystem capability mint. BIOS/no-AHCI media reports unavailable `drs-fs-flags 0x8001C001`.

The `drs-fs-user` checkpoint generalizes that ISO9660 bridge to a bounded path reader and delegates the first scoped read-only filesystem capability to the ring-3 shell principal. UEFI and ISO media prove `drs-fs-user-state 3`, `drs-fs-user-flags 0x001FFFFF`, path `/APPS/LS.APP`, owner `0x00001006`, user owner `0x00000201`, fs binding, cap minting, delegation, broker-routed read, root and `/APPS` directory reads, nonzero file LBA, `drs-fs-user-bytes 79`, checksum `0xFDB1F751`, content match, wrong-owner denial, stale-handle denial, user-buffer delivery, and zero write, commit, or additional filesystem capability mint. BIOS/no-AHCI media reports unavailable `drs-fs-user-flags 0x801C0001`. The parser is still deliberately minimal: primary volume descriptor, root directory, and one root-level directory component, not Joliet, Rock Ridge, path tables, mounting, or writeable filesystem state.

The `drs-fs-shell` checkpoint binds to the existing `drs-fs-user` token and makes the shell descriptor path consume disk-sourced ISO APPS descriptors. UEFI and ISO media prove `drs-fs-shell-state 3`, `drs-fs-shell-flags 0x0001FFFF`, persistent delegation to the shell principal, a dynamic `/APPS` directory scan, eleven staged `.APP` descriptors read and parsed from the current build media, LS/CAT/STAT dispatch proofs, ISO descriptor routing, RAMFS content routing for normal shell file reads/writes, and zero storage write, commit, or additional filesystem capability mint. BIOS/no-AHCI media reports unavailable `drs-fs-shell-flags 0x8000E001`.

The `drs-load` checkpoint binds to the existing `drs-fs-shell` token and proves the first disk-sourced flat-binary utility launch. UEFI and ISO media prove `/APPS/LS.BIN` was read from ISO media, verified as package payload slot 2 (`170` bytes, checksum `0x6B0D4B25`), mapped into a fresh ring-3 region with entry RIP `0x43000010`, launched through the existing transfer path, completed an `ls /` operation through RAMFS authority, and reported `drs-load-source disk` with zero storage write, commit, or additional filesystem capability mint. BIOS/no-AHCI media reports unavailable `drs-load-flags 0x80000701`. The sealed image is now a compact bootstrap/fallback rather than a duplicate utility-code carrier.

The `drs-load-full` checkpoint binds to the existing `drs-load` token and proves full disk-binary utility coverage for the current shell command set. UEFI and ISO media prove ten flat binaries (`CAT.BIN`, `STAT.BIN`, `MKDIR.BIN`, `WRITE.BIN`, `TOUCH.BIN`, `APPEND.BIN`, `COPY.BIN`, `DELETE.BIN`, `RENAME.BIN`, and `MOVE.BIN`) are read from ISO media, verified against package payload slots 3 through 12, registered as disk-sourced launch candidates, and that disk-sourced CAT, MKDIR, WRITE, RENAME, and MOVE utilities launch through the same brokered transfer path. The checkpoint reports `drs-load-full-binaries 10`, `drs-load-full-verified 10`, source `disk`, completed CAT/MKDIR/WRITE/RENAME/MOVE proofs, and zero storage write escalation, commit authority, or additional filesystem capability mint. BIOS/no-AHCI media reports unavailable `drs-load-full-flags 0x80001C01`. The sealed 16 KiB runtime image now carries the persistent ring-3 shell/bootstrap path plus fallback code while utility command bodies stay disk-sourced.

The `drs-pci-ecam` checkpoint moves q35 UEFI and ISO PCI discovery onto ACPI-described PCIe ECAM without adding ambient hardware authority. The UEFI loader records a minimal RSDP/XSDT/MCFG handoff, the x64 PCI scanner uses the MCFG segment-0 bus range for read-only ECAM config reads when present, and BIOS/disk paths continue to report no MCFG while falling back to legacy `0xCF8/0xCFC` I/O-port discovery. Verification requires `drs-pci-ecam-rsdp 1`, `drs-pci-ecam-mcfg 1`, a nonzero ECAM base, and the same AHCI controller found through ECAM on UEFI/ISO media, while preserving the whole storage chain and zero additional write, commit, block, or filesystem authority.

The `drs-apic` checkpoint uses the same ACPI XSDT walk to parse the MADT, find LAPIC and IOAPIC bases, install a dedicated kernel-only APIC MMIO mapping, enable the Local APIC, mask the 8259 PIC on UEFI paths, and route the current PIT, PS/2 keyboard, and AHCI completion interrupt lines through the IOAPIC. Verification requires nonzero LAPIC/IOAPIC bases, `drs-apic-pic-disabled 1`, live timer ticks, live keyboard bytes, and the full storage/shell proof chain still green on UEFI and ISO media; BIOS/disk reports `drs-apic-madt 0` and keeps the legacy PIC route.

The `drs-apic-override` checkpoint extends that minimal MADT parser to consume Interrupt Source Override entries for the ISA IRQ range before programming IOAPIC redirection. UEFI and ISO media now prove the override scan ran, QEMU q35 exposed five overrides, the PIT timer used remapped GSI 2 instead of assuming GSI 0, the keyboard stayed live on GSI 1, AHCI completion routing preserved its GSI/polarity/trigger telemetry, and timer, keyboard, storage, and shell proofs remain green. BIOS/disk remains on the PIC fallback with override scan/count both zero.

The `drs-xhci` checkpoint now performs the real USB keyboard bring-up path without replacing the PS/2 fallback. UEFI and ISO media attach a qemu-xhci USB keyboard, discover class `0x0C/0x03/0x30` through ECAM, map BAR0 through a broker-private kernel MMIO aperture, stage DCBAA, command/event rings, control-transfer memory, and interrupt-transfer memory, perform USBLEGSUP handoff when that extended capability exists, reset and run the controller, enable and address a USB slot, read USB configuration plus HID report descriptors, configure a boot-protocol interrupt endpoint, translate bounded 8-byte keyboard reports to ASCII, and feed those bytes into the existing scoped input service. Real-hardware debug builds also draw the current xHCI/PS2/I2C input state directly to the GOP framebuffer so VirtualBox or laptop failures can be diagnosed without serial output. BIOS/disk media report unavailable and stay on the legacy PS/2/PIC path. No ambient input authority or user-visible USB device authority is minted.

The `drs-mouse` checkpoint adds brokered pointer input without handing the framebuffer or device registers to applications. BIOS/disk and UEFI/ISO media prove a mouse is found, at least one movement packet is accepted, and button state is reported as a bounded value. The PS/2 path enables the 8042 second port, accepts IRQ12/polled packets, and feeds a dedicated mouse queue; the xHCI path recognizes boot-protocol mouse HID interfaces and stages a separate interrupt report ring. The kernel maintains clamped framebuffer-relative cursor coordinates, but no compositor, window manager, ambient pointer device, or direct pixel write authority is introduced by this checkpoint.

The `drs-net` checkpoint adds the first broker-private QEMU network exchange without making networking ambient. UEFI and ISO media attach a modern virtio-net PCI device, discover it through the existing ECAM scanner, parse the virtio common/notify/device configuration capabilities, negotiate MAC support, stage broker-private RX/TX virtqueues and buffers, transmit one ARP request for `10.0.2.2`, receive a matching ARP reply, and report nonzero local/peer MAC telemetry. BIOS/disk media report unavailable. The checkpoint grants zero filesystem authority, zero storage authority, and zero ambient network authority beyond that one brokered exchange.

The `drs-dhcp` checkpoint layers a minimal IPv4/UDP path onto that same broker-private virtio-net queue. UEFI and ISO media build IPv4 headers with checksums, build UDP datagrams, send DHCPDISCOVER from client port `68` to server port `67`, parse DHCPOFFER options, send DHCPREQUEST, parse DHCPACK, and record the assigned QEMU address, gateway `10.0.2.2`, DNS server, and lease time. BIOS/disk media report unavailable. No socket API, reusable endpoint, filesystem authority, storage authority, or ambient network authority is minted.

The loader-budget checkpoint is intentionally host-side only: build output, the scaffold report, the UEFI manifest, and all x64 QEMU verifiers now agree on the 1024-sector BIOS limit, the current 896-sector scaffold, and the 128-sector reserve before accepting disk, UEFI, or ISO media. The build also writes `dist\limitlessos-x86_64.size.txt`, which records final section pressure (`text 356576`, `rodata 99872`, `data 1808`, `bss 430080`) plus the largest object contributors so the next storage/input/network/display checkpoint can target real byte pressure instead of guessing. QMP keyboard and mouse verification keep bounded pacing/settling so the compact scaffold preserves externally visible shell commands and pointer packets as distinct proof events. The loader-budget proof itself adds no unscoped filesystem authority, storage write authority, commit authority, ambient network authority, or ambient input authority.

Apart from the explicitly bounded `drs-mmio`, `drs-dwin`, AHCI `drs-read`, NVMe `drs-nvme-read`, broker-private NVMe `drs-nvme-gpt` partition discovery, broker-private expanded `drs-nvme-fat` file-read and gated mutation proof, scoped shell-owned `drs-nvme-rw` write/commit delegation, read-only `drs-block`, broker-private `drs-fs`, single delegated read-only `drs-fs-user`, disk-sourced `drs-fs-shell` descriptor-read, disk-sourced `drs-load` flat-binary launch, and disk-sourced `drs-load-full` full utility registration/launch storage-side effects, the storage stack still deliberately does not mint ambient or additional filesystem authority, expose storage through ambient paths, create reusable read leases, grant ambient media-read authority, clear interrupts with a nonzero mask as a completion path, wait for AHCI IRQs, queue multi-slot block commands, dispatch worker-owned block jobs, publish NVMe block endpoints, create format authority, or provide any storage write/commit authority outside the single audited shell capability.






